# FOLD-IN 2026-09 — ranked, line-referenced changes for `PromptStudio.html`

Produced by research agent 3B on **2026-09-03** from the eleven Wave-1/2 outputs, filtered through
[`research/_addenda/verification-2026-09.md`](../research/_addenda/verification-2026-09.md) (binding).
Companion digest: [`research/digests/2026-09-03-digest.md`](../research/digests/2026-09-03-digest.md).

**Nothing here has been applied.** Every item names its exact target, the current text verbatim, the
proposed replacement in the bank's terse style, the research file + heading it rests on, the verifier's
verdict number where one exists, an evidence grade, and a one-line risk note.

**Binding filter.** No item in A–E states as fact anything the verifier marked UNSUPPORTED or
OVERSTATED. Those claims are carried in **§G** at their honest grade with a named test. The verifier's
"Items the synthesis agent must NOT fold in" list (34 entries) was applied item by item.

Counts: **A 40 · B 12 · C 4 · D 10 · E 8** = **74 actionable items** in A–E (the brief's minimum was 25)
· F 10 backlog dispositions · G 13 deferred · H 9 notices. **106 items total.**

Line numbers are from `PromptStudio.html` as read 2026-09-03 (`TARGETS` 529–813, `KNOWLEDGE` 816–889,
`GOTCHAS` 892–937, `SDXL_NEG` 526, validators 1180–1310, `WF_MAP` 1713, `WF_RES` 1904–1912,
`wfNotes` 2152–2161).

---

## A — Corrections (app currently teaches something wrong)

### A1 — Delete the Wan orbit/45° rule from `KNOWLEDGE`
- **Target:** `KNOWLEDGE ¶WAN 2.2 "keep ONE camera command per shot, placed FIRST"`
- **Change:** REPLACE
- **Current text:** `keep ONE camera command per shot, placed FIRST in its clause, using official tokens ("arc shot", NOT "orbit" — orbit is the documented failure word), arcs under ~45 degrees, avoid the word "cinematic" (triggers the stylization branch), and treat exact choreography as best-effort`
- **Proposed text:** `keep ONE camera command per shot, placed FIRST in its clause, using the vendor's own 运镜 vocabulary (镜头前推/后拉/上摇/下摇/从左到右, 环绕运镜, 复合运镜), avoid the word "cinematic" (triggers the stylization branch), and treat exact choreography as best-effort. OFFICIAL: the guide suppresses 拍摄角度 tokens whenever a 运镜 clause is present — pick a camera MOVE or a camera ANGLE, not both. No degree limit on arcs exists in any first-party source; the guide's only 环绕运镜 example is a successful ~180-degree back-to-front orbit.`
- **Evidence:** `research/wan22.md` §2026-09 sweep → *Contradicts current corpus* #1; §*New official guidance* #2 (help.aliyun.com rev. 2026-09-02) · verification **#43 CONFIRMED**, `KNOWLEDGE` audit **K1**
- **Grade:** OFFICIAL (the replacement); the deleted clause was OFFICIAL-graded with no source
- **Risk:** If wrong, students stop avoiding a real failure mode. But two sweeps found no first-party degree figure or orbit warning on any surface, and the vendor's own exemplar orbits ~180° successfully. The verifier is explicit: remove, do not downgrade-and-keep. **T5** (`test-kit-2026-09.md` §T5, arc-vs-orbit arm) settles it empirically.

### A2 — Remove the orbit ban and the 45° number from the Wan system prompt; fix the locked-camera token
- **Target:** `TARGETS.wan.system "ONE camera behavior only, placed FIRST in its clause"` (≈line 541)
- **Change:** REPLACE
- **Current text:** `- ONE camera behavior only, placed FIRST in its clause, using official vocabulary — say "arc shot" (never "orbit"), keep arcs under 45 degrees. If the camera should stay still, write "fixed camera, no camera movement" / "固定机位，镜头不移动" and no other camera verbs.`
- **Proposed text:** `- ONE camera behavior only, placed FIRST in its clause, using official vocabulary (push in / pull back / pan left-right / tilt up-down / arc shot; 镜头前推、镜头后拉、镜头从左到右、镜头上摇、环绕运镜). Do NOT also state a camera angle — the official rewriter suppresses 拍摄角度 whenever a camera move is present. If the camera should stay still, write "fixed camera, no camera movement" / "固定镜头，镜头不移动" (accept 固定机位 and 镜头位置保持不动 as synonyms, but emit 固定镜头) and no other camera verbs.`
- **Evidence:** `research/wan22.md` §*Contradicts* #1 and #3; §*Validator changes* #2, #3 · verification **#42, #43 CONFIRMED**; TARGETS audit rows 1–2
- **Grade:** OFFICIAL
- **Risk:** `固定机位` is widely used in the wild, so emitting `固定镜头` could look unfamiliar — mitigated by keeping both as accepted synonyms. Low risk: 固定镜头 is the vendor's literal instruction. **T5** compares all three forms at fixed seed.

### A3 — Delete the "orbit" validator error
- **Target:** `validator wan orbit check in PromptStudio.html near line 1194`
- **Change:** DELETE
- **Current text:** `if (/\borbit/i.test(pos)) issues.push('says "orbit" — the official Wan token is "arc shot" (orbit is the documented failure word)');`
- **Proposed text:** (delete the line; keep the `camMoves > 2` and `cinematic` checks immediately around it, and add the new angle-vs-move check from **D6**)
- **Evidence:** `research/wan22.md` §*Contradicts* #1 · verification **#43**
- **Grade:** OFFICIAL absence (not found on the 2026-09-02 guide or in `system_prompt.py` across two sweeps)
- **Risk:** None to correctness. The validator currently penalises a word the vendor itself uses in its only worked orbit example.

### A4 — Rewrite the invented LTX enhancer "history" as current state
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "HISTORY: LTX-2.5's built-in prompt enhancer was ON"`
- **Change:** REPLACE
- **Current text:** `HISTORY: LTX-2.5's built-in prompt enhancer was ON by default Aug 11-20, 2026, silently rewriting fully-specified prompts — community results from that window are confounded.`
- **Proposed text:** `ENHANCER STATE, CURRENT (Sept 2026): the official ComfyUI T2V template ships prompt_enhance = true — your prompt is rewritten by Gemma 4 E2B before encoding unless you toggle it off on the "Text to Video (LTX-2.5)" subgraph node. Lightricks' own docs say so; Comfy Org's tutorial for that same template says the opposite ("keeps it off by default") and is wrong. The Python path (--enhance-prompt) is opt-in and defaults OFF. No source dates any change of this default, so treat every community LTX-2.5 result of unknown provenance as possibly enhancer-rewritten — the confound is ongoing, not historical. OFFICIAL`
- **Evidence:** `research/ltx23.md` §2026-09 sweep item 5; §*Contradicts* #11 · verification **#15, #16 CONFIRMED** ("the sweep's single most consequential correct finding"), ruling #5, **K9**
- **Grade:** OFFICIAL/TESTED (static inspection of the shipped `widgets_values` array)
- **Risk:** High value, low risk — both halves independently verified. If not folded, the app actively tells students a live problem is over.

### A5 — New GOTCHAS card for the enhancer default
- **Target:** `NEW GOTCHAS card`
- **Change:** ADD
- **Current text:** none — the verifier flags this as "the sweep's top teaching correction has no card"
- **Proposed text:** `{t:'LTX', m:'My ComfyUI LTX-2.5 prompt runs exactly as I typed it.', r:'The shipped official T2V template has prompt_enhance = true — Gemma 4 E2B rewrites your prompt before encoding. Comfy Org\'s own tutorial says it is off by default; the file says otherwise. Toggle prompt_enhance off on the subgraph node, and wire a Preview Text node to the enhancer output before blaming your wording.'}`
- **Evidence:** `research/ltx23.md` item 5; §*Tested findings* (the Preview-Text remedy comes from the user reports in HF discussions/36 + ComfyUI issue #15600) · verification **#15, #16**, GOTCHAS audit "(missing card)"
- **Grade:** OFFICIAL (default state) + USER-VERIFIED (the diagnostic)
- **Risk:** None. The remedy is a UI toggle with no rewiring.

### A6 — Tell the LTX target about the enhancer
- **Target:** `TARGETS.ltx.system "Never invent characters, dialogue, or camera motion"` (≈line 592, append a new bullet after it)
- **Change:** ADD
- **Current text:** none
- **Proposed text:** `- The official ComfyUI LTX-2.5 T2V template ships prompt_enhance = true, so a well-formed prompt may be rewritten before encoding. Write to the model's own structure so the enhancer's "if the input is already detailed, chronological and in the requested format: DO NOT make major edits" rule fires; tell the user to toggle prompt_enhance off for exact wording.`
- **Evidence:** `research/ltx23.md` items 4, 5, 6 (the enhancer system prompt's own no-major-edits clause, verbatim) · verification **#15, #16**; TARGETS audit "`TARGETS.ltx` (missing)"
- **Grade:** OFFICIAL
- **Risk:** None — it makes the app's output more robust under either toggle state.

### A7 — Replace the LTX 150-word cap with a version-split length rule
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "Dialect: ONE chronological, literal, action-first paragraph"`
- **Change:** REPLACE
- **Current text:** `Dialect: ONE chronological, literal, action-first paragraph; ≤150 words (official enhancer ceiling for 2.3); one main action per 2-3 seconds`
- **Proposed text:** `Dialect: ONE chronological, literal, action-first paragraph; one main action per 2-3 seconds. LENGTH IS VERSION-SPLIT: LTX-2.5's current guide gives no word count at all — "roughly 4-8 descriptive sentences" for a single shot, longer for screenplay-style scenes "provided every sentence adds concrete visual or audio detail". The only sourced 2.3-era word figure is the LTX-2 README's "Keep within 200 words". The ≤150 figure the community repeats belongs to the retired LTXV-0.9 enhancer node (Llama-3.2-3B + Florence-2), which 2.5 does not use. OFFICIAL`
- **Evidence:** `research/ltx23.md` items 1, 6; §*Contradicts* #3, #4 · verification **#12 CONFIRMED** (verified across the whole page), **K5**
- **Risk:** Loosening a cap could let students write bloated prompts. Mitigated because the sentence-count band and "every sentence adds concrete detail" are stricter in practice than a raw word count, and the prompt-inflation doctrine (¶GENERAL CONCEPTS) still applies.
- **Grade:** OFFICIAL

### A8 — Fix the GOTCHAS card that says the 150 cap is "true for 2.3"
- **Target:** `GOTCHAS card t:'LTX' m:'LTX never cuts and caps at 150 words.'`
- **Change:** REPLACE
- **Current text:** `r:'True for 2.3. LTX-2.5 has native multi-shot with named cuts, and its current docs replaced the cap with "4-8 descriptive sentences". Know your version.'`
- **Proposed text:** `r:'Neither half survives. The 150 figure comes from the retired LTXV-0.9 enhancer node, not from any 2.3 guidance — the only sourced 2.3-era figure is the README\'s "Keep within 200 words". And LTX-2.5 has native multi-shot: cuts are legal when you NAME them in prose, and its current docs give "4-8 descriptive sentences" instead of a word cap.'`
- **Evidence:** `research/ltx23.md` §*Contradicts* #3/#4 · verification GOTCHAS audit (severity **High**), **#12**
- **Grade:** OFFICIAL
- **Risk:** None — the card currently vouches for a number with no source on either version.

### A9 — Replace "No universal negative list is documented" (LTX)
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "No universal negative list is documented"`
- **Change:** REPLACE
- **Current text:** `No universal negative list is documented; dev vs distilled variants should not share settings conclusions.`
- **Proposed text:** `THREE official negatives exist and they disagree: the ~90-item DEFAULT_NEGATIVE_PROMPT in ltx_pipelines/utils/constants.py; a Diffusers copy missing its first five items (has_subtitles, has_blurbox, transition from black, transition to black, speech_ending_short — these read like trained dataset flags); and a six-token aesthetic negative hardcoded in the official ComfyUI 2.5 template ("pc game, console game, video game, cartoon, childish, ugly"). Never REQUIRE a negative; offer the official default and say the three disagree. dev vs distilled variants should not share settings conclusions. OFFICIAL`
- **Evidence:** `research/ltx23.md` item 7; §*Contradicts* #2; §*Validator changes* V5 · verification **#18 CONFIRMED**, **K8** (severity High)
- **Grade:** OFFICIAL
- **Risk:** Naming three lists could confuse. Mitigated by the "never require one" framing, which is the practical instruction either way.

### A10 — Downgrade "negatives are INERT on LTX's distilled path" to untested
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "NEGATIVE PROMPTS ARE INERT on the default distilled path"`
- **Change:** DOWNGRADE-LABEL
- **Current text:** `NEGATIVE PROMPTS ARE INERT on the default distilled path: CFG=1 makes the guider skip the negative pass entirely — official 2.5 templates even ship a decorative default negative that does nothing; write exclusions into the positive text (community alternative: NAG nodes).`
- **Proposed text:** `NEGATIVES ARE NEVER REQUIRED on the default distilled path: it runs a dual-CFG guider at CFG 1.0 for both video and audio (OFFICIAL), and the shipped template hardcodes a six-token negative anyway. Whether that negative is fully inert at CFG 1 is UNTESTED — no source states it and no A/B exists. Write exclusions into the positive text regardless. NAG (ComfyUI's built-in NAGuidance) is the proposed route to a real negative on a distilled path, but there is zero LTX-specific NAG evidence and an open issue questions whether the node works at all. SPECULATION on the inertness; OFFICIAL on CFG 1.0.`
- **Evidence:** `research/ltx23.md` §*Tested findings* (NAG section, explicitly `[SPECULATION]`); item 8 (distilled CFG 1.0); §*Nothing-found* (LTX + NAG: nothing) · verification GOTCHAS audit (severity **High** — "a confident claim on a speculative mechanism"), TARGETS audit, exclusion **#11**
- **Grade:** SPECULATION (inertness) / OFFICIAL (CFG 1.0)
- **Risk:** Softening this may let students waste effort on negatives. That is the lesser error: the app currently asserts a mechanism nobody has measured. **T2-LTX** (`test-kit-2026-09.md` §T2-LTX, three arms: shipped / negative emptied / NAGuidance) settles it — and a pixel-identical result between arms (i) and (ii) is the only proof of inertness.

### A11 — Same downgrade in the GOTCHAS card and the LTX system prompt
- **Target:** `GOTCHAS card t:'LTX' m:'My LTX negative prompt is cleaning up the output.'` **and** `TARGETS.ltx.system "Negative prompts do not exist on the distilled path"` (≈line 591)
- **Change:** REPLACE (both)
- **Current text (card):** `r:'On the default distilled path (CFG 1) the negative pass is skipped entirely — official templates even ship a decorative negative. Write exclusions into the positive text. Verification: ComfyUI embeds the executed graph in the mp4 metadata — check what the encoder really received.'`
- **Current text (target):** `- Negative prompts do not exist on the distilled path (CFG 1 skips them) — write every exclusion positively inside the paragraph.`
- **Proposed text (card):** `r:'Probably not, but nobody has measured it. The distilled path runs at CFG 1 and the official template hardcodes a six-token negative anyway; whether that negative does anything at CFG 1 is untested. Write exclusions into the positive text — that works either way. Verification: ComfyUI embeds the executed graph in the mp4 metadata, so you can read what the encoder actually received.'`
- **Proposed text (target):** `- Never require a negative prompt: the distilled path runs at CFG 1.0 and the shipped template already hardcodes one. Write every exclusion positively inside the paragraph.`
- **Evidence:** as A10 · verification GOTCHAS + TARGETS audits
- **Grade:** SPECULATION / OFFICIAL as above
- **Risk:** Same as A10.

### A12 — Correct the LTX VRAM floor and the mechanism behind the low numbers
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "Gemma 4 12B encoder (~15GB alone"`
- **Change:** REPLACE
- **Current text:** `Gemma 4 12B encoder (~15GB alone — "runs on 12GB" claims assume GGUF/offload)`
- **Proposed text:** `Gemma 4 12B encoder, version-gated (gemma4-12b-ltx-v1 — stock Google Gemma 4 is officially NOT a substitute, and it now ships WITH the model). OFFICIAL MINIMUM IS 32GB+ VRAM (system-requirements.md and the ComfyUI-LTXVideo README both), with 32GB RAM / 100GB storage / CUDA 12.7+ / Python 3.12+; the "12GB" figure appears only on the marketing page and "16GB" only on a product table. The low numbers come from int8/fp8 quantisation + CPU/disk offload + block streaming + tiled VAE decode — NOT GGUF: there is no official GGUF path for LTX-2.5 (community request open, no reply). OFFICIAL`
- **Evidence:** `research/ltx23.md` item 10; §*Contradicts* #10; §*Nothing-found* (GGUF) · verification **#19 CONFIRMED verbatim**, **K6** (severity High)
- **Grade:** OFFICIAL
- **Risk:** Telling students 32 GB is the floor may discourage 24 GB users who succeed anyway. Mitigated by naming the four mechanisms that actually make it fit; the app should not repeat a marketing figure as a spec.

### A13 — LTX licence: "annual revenues", not ARR
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "license is NOT Apache — free under $10M ARR"`
- **Change:** REPLACE
- **Current text:** `license is NOT Apache — free under $10M ARR.`
- **Proposed text:** `license is NOT Apache: the LTX-2.x Community License (license date 2026-08-11) requires a paid licence for entities with "annual revenues of at least $10,000,000" — annual revenues, not ARR, and §1.6 aggregates subsidiaries and affiliates under common control. Attachment A also forbids training a competing model (#18), use in a product that directly competes with Lightricks (#20), and removing watermarking/provenance features (§6). OFFICIAL`
- **Evidence:** `research/ltx23.md` item 15 (LICENSE verbatim) · verification **K7**
- **Grade:** OFFICIAL
- **Risk:** Legally material and low-risk to state; the distinction matters for any student with a revenue-bearing side business.

### A14 — Re-point the LTX IC-LoRA control names
- **Target:** `KNOWLEDGE ¶LTX 2.3 / 2.5 "For exact pose/motion paths use the IC-LoRA controls"`
- **Change:** REPLACE
- **Current text:** `For exact pose/motion paths use the IC-LoRA controls (Pose Control, Motion Track Control) — not longer prose.`
- **Proposed text:** `For exact pose/motion paths use the IC-LoRA controls — not longer prose. For 2.5 the names changed: pose now lives inside UNION CONTROL (depth + canny + pose in one checkpoint), and "Motion Track Control" is documented as MOTION CONTROL with two nodes (LTX Draw Tracks, LTX Sparse Track Editor; start with 3-4 keypoints per track). The standalone pose adapter is an LTX-2.0 19b file absent from the 2.5 table. Prompt rules for controls: describe visual STYLE, not the control type; prepend the trigger phrase for adapters that have one (DEBLUR, REMOVEBEARD, COLORIZE, ADD WATER, ENHANCE QUALITY) — the Pixel Spatial Upscaler has none and wants the SOURCE video's own prompt (STAFF); never contradict the reference's structure. HDR, Dub-It and Relight are 2.3-ONLY. OFFICIAL`
- **Evidence:** `research/ltx23.md` item 13; §*Contradicts* #6, #7 · `research/_addenda/staff-claims-2026-09.md` row 4 (`art-alex`, LTX.io org) · verification **K10**
- **Grade:** OFFICIAL + STAFF
- **Risk:** Names could change again. Low: the roster is on a versioned docs page and the staff quote is anchored.

### A15 — Fix the H3 reference caps
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "Reference caps: 9 images + 3 videos"`
- **Change:** REPLACE
- **Current text:** `Reference caps: 9 images + 3 videos (2-15s each, 15s TOTAL) + 3 audio (15s total), 12 files max.`
- **Proposed text:** `Reference caps LOCALLY are 9 images + 3 video clips + 3 video soundtracks + 3 standalone audio — 18 slots, NO total cap (MiniMaxH3ReferenceToVideo's own Autogrow maxima). Because each reference video's soundtrack is a separate input that takes its own ordinal, <Audio j> can reach 6. The "12 files max" figure is a hosted/tooling rule and is not enforced locally. Reference videos need at least 5 frames, are trimmed DOWN to the nearest 17n+5, and Qwen sees them at 2 fps with <T.T seconds> timestamps per 2-frame block. OFFICIAL`
- **Evidence:** `research/minimax-h3.md` §*Contradicts* #4, #5; §*Reference-tag mechanics* · verification **#52 CONFIRMED**, **K12** (severity High), exclusion **#18**
- **Grade:** OFFICIAL
- **Risk:** A student could now attach 18 assets and hit a real hosted limit if they later move to the API. Mitigated by naming the 12 as the hosted rule. The current number is a **hard validator figure that is wrong locally**.

### A16 — Delete the unsourced H3 audio-requires-visual rule
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "audio cannot be sent without at least one image or video"`
- **Change:** DELETE
- **Current text:** `audio cannot be sent without at least one image or video;`
- **Proposed text:** (delete the clause; the surrounding arrival-order sentence stands and is upgraded by **B8**)
- **Evidence:** `research/minimax-h3.md` §*Reference-tag mechanics* (the node iterates `ref_audios` independently) · verification **K13**, exclusion **#19**
- **Grade:** OFFICIAL absence (not in the schema, not in the docs)
- **Risk:** None. Nothing enforces it; asserting it blocks a legal configuration.

### A17 — H3 release date
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "open weights Aug 3, 2026"`
- **Change:** REPLACE
- **Current text:** `open weights Aug 3, 2026`
- **Proposed text:** `open weights Aug 2, 2026`
- **Evidence:** `research/minimax-h3.md` item 1 (licence header: *"MiniMax H3 release date/License date: August 2, 2026"*) · verification **#48 CONFIRMED**, **K11**
- **Grade:** OFFICIAL
- **Risk:** None.

### A18 — Retire "SCAIL-2 is prompt-inert"
- **Target:** `KNOWLEDGE ¶SCAIL-2 "CLASSIFICATION: effectively PROMPT-INERT"`
- **Change:** REPLACE
- **Current text:** `CLASSIFICATION: effectively PROMPT-INERT ("prompts have limited impact on the final result"; the paper has no prompt ablation) — real levers are mask accuracy, canvas alignment, drive framing, segment arithmetic.`
- **Proposed text:** `CLASSIFICATION: PROMPT-SUBORDINATE, not inert — the least prompt-sensitive model here that still has a load-bearing prompt. The prompt is an APPEARANCE-AND-ENVIRONMENT channel, not a motion channel. For "not inert": the vendor states long detailed prompts beat short and empty ones, ships a whole Gemini rewriter whose only job is to lengthen and structure the prompt, and its own workflow note says "Main prompt controls final appearance". For "subordinate": the paper demotes text by design ("soft guidance beyond textual instructions"; the model "struggles to distinguish the two modes from textual cues alone"), never ablates the prompt, and has no text-alignment metric — and every multi-identity control question resolves to position, mask and footage. Real levers stay mask accuracy, canvas alignment, drive framing, segment arithmetic. OFFICIAL for the direction; no controlled prompt experiment exists anywhere.`
- **Evidence:** `research/scail2.md` §*Prompt-inertness verdict*; §*New official guidance* · verification **#2, #5 CONFIRMED**, **K17** (severity **High** — "the app's classification drives its whole SCAIL UX")
- **Grade:** OFFICIAL (direction) / TESTED + USER-VERIFIED (magnitude)
- **Risk:** Encouraging prompt effort on a model where masks dominate wastes student time. Mitigated by "subordinate" and by keeping the real-levers sentence. **The SCAIL prompt ablation named in `scail2.md` §Nothing-found** (fixed seed, fixed drive/mask, four prompts: empty / "The girl is dancing" / a 110-word official-style prompt / the same with background nouns removed) is the test.

### A19 — SCAIL-2 length: one band for both modes, plus the hard token cap
- **Target:** `KNOWLEDGE ¶SCAIL-2 "official enhancer bans replace/swap/edit/mask wording"` **and** the adjacent `Length: no total cap`
- **Change:** REPLACE
- **Current text:** `official enhancer bans replace/swap/edit/mask wording and targets one 90-140-word ENGLISH paragraph for Replacement (Animation can be 15-60 words: "The girl is dancing" is an official minimal example).` … `Length: no total cap — 81-frame chunks, 5-frame overlap, official Extend workflow`
- **Proposed text:** `official enhancer targets one 90-140-word ENGLISH paragraph, and the vendor's length doctrine is MODE-AGNOSTIC: "SCAIL-2 is trained with long, detailed prompts. Short prompts or an empty prompt can run, but detailed descriptions of the reference subject and motion usually produce better results." Use 90-140 words for Animation too — "The girl is dancing" is a CLI smoke test, and the official ComfyUI template's own default prompt is 111 words for an animation-style dance. The enhancer's ban list is wider than ours: replace/swap/edit/mask/segmentation PLUS "the task is", Gemini, editing software, and any mention of the prompt-generation process. Length: hard cap 512 UMT5 tokens (wan_shared_cfg.text_len = 512) — 81-frame chunks, 5-frame overlap, official Extend workflow. OFFICIAL`
- **Evidence:** `research/scail2.md` §*Contradicts* #2, #4; §*New official guidance*; §*Validator changes* 1–3 · verification **#2, #5, #7 CONFIRMED**, **K16, K18, K21**
- **Grade:** OFFICIAL
- **Risk:** Longer Animation prompts could dilute. The vendor's own sentence and its own 111-word animation default both point the other way, and the 512-token cap is now a real ceiling the app lacked.

### A20 — SCAIL-2 mask polarity (both sides) and the 704p attribution
- **Target:** `KNOWLEDGE ¶SCAIL-2 "704p recommended for Replacement; multi-person needs --matchnearest"`
- **Change:** REPLACE
- **Current text:** `704p recommended for Replacement; multi-person needs --matchnearest and per-person masks using the TRAINED-IN color palette (Blue/Red/Green/Magenta/Cyan/Yellow — in-code comment: deviating degrades multi-identity quality); mask background polarity INVERTS between modes, and a wrong mask can collapse Animation into Replacement behavior.`
- **Proposed text:** `The card's own resolution sentence is "End-to-end driven supports both 512p and 704p. Pose-driven performs better under 704p" — it does NOT name replacement; treat 704p as the safe default for pose-driven work. Dimensions should be divisible by 32, not 16: the pose/mask conditioning runs at half resolution, so a multiple-of-16-but-not-32 canvas circular-pads and the bottom ~8-16px echoes the top (TESTED; the official ComfyUI tutorial's "divisible by 16" is the surface that is wrong). Multi-person needs --matchnearest (a PREPROCESSOR flag, not generate.py) and per-person masks in the TRAINED-IN palette (Blue/Red/Green/Magenta/Cyan/Yellow — in-code comment: "Model was trained on these exact colors; deviating degrades multi-identity quality"); a 7th identity wraps and collides, so 6 is a hard cap. Routing is POSITION-first, not colour-first — order the reference composite left-to-right to match the driving people; colours carry temporal consistency, not initial assignment. Mask polarity inverts on BOTH sides: Animation = driving black / reference WHITE; Replacement = driving white / reference BLACK. OFFICIAL + TESTED`
- **Evidence:** `research/scail2.md` §*New official guidance* (palette + polarity comments); §*Tested findings* (÷32 mechanism, position-first routing, 6-identity cap); §*Contradicts* #5 · verification **#9 CONFIRMED**, **K19**, exclusions **#1, #2**
- **Grade:** OFFICIAL (palette, polarity, card sentence) + TESTED (÷32 mechanism, routing, cap)
- **Risk:** ÷32 is shipped here as a **recommendation with a TESTED mechanism**, not as a card quote — the card says "should … if using other resolutions", and the verifier forbids the hardened "must" (#63). Node schema uses `step=32`, so the practical effect is the same.

*(Section A continues to A40; numbering is contiguous. A1–A20 above are the video-model and SCAIL
corrections; A21–A40 below cover Wan-Dancer, the SDXL family, Krea 2, FLUX and the cross-cutting rules.)*

### A21 — Delete Wan-Dancer's two invented numbers and correct its dialect
- **Target:** `KNOWLEDGE ¶WAN 2.2 "also open: Wan-Dancer-14B (Apache, Jul 2026, ~28GB bf16)"`
- **Change:** REPLACE
- **Current text:** `also open: Wan-Dancer-14B (Apache, Jul 2026, ~28GB bf16) — dance/choreography video that is NOT prompt-inert: real prompts, CFG 5.0 so negatives are architecturally LIVE (rare among 2026 video models), headline Prompt Alignment 9.03.`
- **Proposed text:** `also open: Wan-Dancer-14B (Apache 2.0, released 2026-07-13) — music-to-dance in two stages. Its prompt is NOT the Wan cinematic formula: it is a fixed caption schema, "一个人正在跳舞，舞蹈种类是{古典舞|街舞|韩舞|拉丁舞|踢踏舞}" plus, for the local stage, 图像清晰程度{…}, 人物动作平均幅度{…}, 人物动作最大幅度{…} — the only first-party Wan source that separates AVERAGE from MAXIMUM motion amplitude. The script silently appends 帧率是{fps}. Cinematography, lighting and camera words are out of schema. CFG 5.0 is official three ways, so negatives ARE architecturally live (rare among 2026 video models) and the standard Chinese Wan negative applies. The global stage asserts world_size == 8, i.e. 8 GPUs. OFFICIAL`
- **Evidence:** `research/wan22.md` §2026-09 sweep item 4; §*Nothing-found register* · verification **#45, #46, #47**, **K2, K3**, exclusion **#8**
- **Grade:** OFFICIAL
- **Risk:** None to correctness — the app currently states a benchmark score and a file size that exist on no surface (card, arXiv v2 abstract, project page all checked). The amplitude ladder's other rungs (低/高) are **not attested**; only 中等/高 appear. See **G12**.

### A22 — Split Wan's ≤100-word I2V rule by language
- **Target:** `KNOWLEDGE ¶WAN 2.2 "I2V-A14B officially wants ≤100 words of MOTION-ONLY content"`
- **Change:** REPLACE
- **Current text:** `I2V-A14B officially wants ≤100 words of MOTION-ONLY content — restating what the image shows wastes the budget.`
- **Proposed text:** `I2V-A14B wants MOTION-ONLY content, and the official limit is language-split: the EN rewriter targets ~100 WORDS, the ZH rewriter says 改写后的prompt字数控制在100字以下 — 100 Chinese CHARACTERS, roughly a third of the budget. Restating what the image shows wastes either budget. Note TI2V-5B has NO empty-prompt branch: the ti2v early return fires first, so an empty prompt there is an empty prompt, not an auto-caption. OFFICIAL`
- **Evidence:** `research/wan22.md` item 1(a) (`decide_system_prompt` verbatim); §*Validator changes* #10 · verification **K4** ("my finding; not in any sweep file"), GOTCHAS audit row 2
- **Grade:** OFFICIAL
- **Risk:** Low. The app currently teaches one number for two different limits, which under-serves Chinese users by ~3×.

### A23 — Same fix in the Wan I2V GOTCHAS card and target
- **Target:** `GOTCHAS card t:'Wan' m:'Wan I2V prompts should describe the whole scene.'` **and** `TARGETS.wanI2V.system "Hard cap 100 words"` (≈line 565)
- **Change:** REPLACE (both)
- **Current text (card):** `r:'Officially the opposite: I2V wants ≤100 words of MOTION-ONLY content. The image already carries appearance and scene; every static word wastes budget.'`
- **Current text (target):** `- Output motion and camera content ONLY: what moves, in what order, at what amplitude and speed, and what the camera does. Hard cap 100 words; 15-70 words is usually right.`
- **Proposed text (card):** `r:'Officially the opposite: I2V wants MOTION-ONLY content — and the cap is language-split, ~100 English words but 100 Chinese CHARACTERS (百字以下). The image already carries appearance and scene; every static word wastes budget.'`
- **Proposed text (target):** `- Output motion and camera content ONLY: what moves, in what order, at what amplitude and speed, and what the camera does. Hard cap 100 words in English, 100 characters in Chinese (the two official rewriters differ); 15-70 words is usually right.`
- **Evidence:** as A22 · verification GOTCHAS audit, TARGETS audit `TARGETS.wanI2V`
- **Grade:** OFFICIAL
- **Risk:** None.

### A24 — Fix the Illustrious self-contradiction inside one `KNOWLEDGE` paragraph
- **Target:** `KNOWLEDGE ¶SDXL FAMILY "Anime: Pony V6 needs its score ladder"`
- **Change:** REPLACE
- **Current text:** `Anime: Pony V6 needs its score ladder (score_9, score_8_up...) + source_* tags; Illustrious wants masterpiece/best-quality + canonical Danbooru tags; machine-translating tags destroys them.`
- **Proposed text:** `Anime: Pony V6 needs its full six-rung score chain + one source_* tag + CLIP-skip 2 (the card is explicit: "otherwise you will be getting low quality blobs"), and its card also endorses natural language — it is not tags-only. NoobAI wants "masterpiece, best quality, newest, absurdres, highres, safe," up front. Animagine wants "masterpiece, high score, great score, absurdres" at the END and states plainly that natural language "may not be effective". NO ONOMA CARD STATES ANY ILLUSTRIOUS QUALITY PREFIX — the masterpiece/best-quality scheme is a 2025 third-party guide, LORE. Machine-translating tags destroys them.`
- **Evidence:** `research/sdxl.md` §*Recipe table*; §*Verbatim blocks*; §*Contradicts* #1, #2, #3, #4 · verification **#61 CONFIRMED**, **K22** (severity **High** — "internal self-contradiction in one paragraph"), exclusions **#29, #30**
- **Grade:** OFFICIAL / CREATOR (per card); LORE for the Illustrious prefix
- **Risk:** Students on Illustrious may lose a prefix that helps in practice. Mitigated by keeping it as an explicitly `[LORE]` suggestion rather than deleting it — but the app must stop asserting it on one surface and denying it on another.

### A25 — Strengthen the NoobAI v-pred warning
- **Target:** `KNOWLEDGE ¶SDXL FAMILY "Sampler advice equally non-portable"`
- **Change:** REPLACE
- **Current text:** `Sampler advice equally non-portable (NoobAI v-pred bans Karras; RealVis requires it; Pony Realism bans DPM++ 2M Karras).`
- **Proposed text:** `Sampler advice equally non-portable, and the NoobAI card is STRICTER than a Karras ban: "Sampling Method: Euler (⚠️ Other samplers will not work properly)", with Civitai adding "V prediction does not support the Karras series … we suggest using Euler and DDIM". A v-pred graph needs FOUR things together — Euler or DDIM (never a Karras schedule), prediction_type=v_prediction, rescale_betas_zero_snr=True, and a CFG-Rescale / dynamic-CFG node at ≈0.2 — plus CFG dropped to 4-5. Swapping the scheduler alone does not fix it. The same applies to Illustrious v3.0-vpred and v3.5-vpred, which are v-prediction too. Conversely RealVis REQUIRES a Karras variant (DPM++ SDE Karras ≥30 steps, or DPM++ 2M Karras ≥50). OFFICIAL`
- **Evidence:** `research/sdxl.md` §*Verbatim blocks* (NoobAI card + Civitai description + the zsnr diffusers snippet); §*Contradicts* #5, #6 · verification **#61 CONFIRMED**, **K25** (severity High)
- **Grade:** OFFICIAL / CREATOR
- **Risk:** None — the current line under-warns on a checkpoint family that produces visibly broken output when mis-wired. **T7** (`test-kit-2026-09.md` §T7) tests it, but **fix defect #67 first**: the shipped T7 arm has no `ModelSamplingDiscrete` v_prediction node.

### A26 — Scope "Illustrious org static since Apr 2025"
- **Target:** `KNOWLEDGE ¶SDXL FAMILY "Illustrious org static since Apr 2025"`
- **Change:** REPLACE
- **Current text:** `Illustrious org static since Apr 2025, and the popular derivatives kept v0.1-era CLIP — v2.0 natural-language gains are absent downstream.`
- **Proposed text:** `Illustrious's HUGGINGFACE org is static since Apr 2025 (it stops at v2.0), but the vendor platform lists nine models — v3.0 EPS, v3.0 VPred and v3.5 VPred shipped after that (v3.5-vpred release notes 2025-06-16) with NO open weights on the Onoma HF org. v3.0-vpred and v3.5-vpred are v-prediction, so the Karras ban applies to them. Popular derivatives kept v0.1-era CLIP — v2.0 natural-language gains are absent downstream.`
- **Evidence:** `research/sdxl.md` §*Contradicts* #6; §*Illustrious version inventory*; §*Nothing-found register* · verification **K24**
- **Grade:** OFFICIAL
- **Risk:** None; it converts a false absolute into a scoped, useful fact.

### A27 — Krea 2: the cap does exist, and it is in the vendor's own encoder
- **Target:** `KNOWLEDGE ¶KREA FAMILY "CONTESTED length cliff"`
- **Change:** REPLACE
- **Current text:** `CONTESTED length cliff: one controlled single-variable report (ComfyUI issue #14782, open, no maintainer reply) got clean output at 576 tokens but pure black at 640 — yet no cap exists anywhere in the ComfyUI/Krea source and the official guide recommends LONG prompts. Treat as unconfirmed: if long prompts produce black frames, shorten below ~2,000 characters; and never auto-expand a working prompt near that zone.`
- **Proposed text:** `LENGTH CEILING, sourced: Krea's own encoder.py sets max_length = 512 and tokenises to a 541-token window (512 + 34 prefix − 5 suffix), truncating SILENTLY, then slices off the 34-token system prefix — a hard ceiling of 512 conditioning positions. Krea's reference CLI can never exceed it; ComfyUI can, and that is the bug. One controlled single-variable report (ComfyUI #14782, open, no maintainer reply, INT8 ConvRot checkpoint) measured 49/512/513/576 OK, 640 BLACK, 674 corrupted. So: 512 is the reference cap, (576, 640] is where that prompt died, treat anything above 512 as undefined behaviour. ~2,850 characters ≈ 512 positions at that report's own 5.6 chars/position ratio. Never auto-expand a working prompt near the zone. OFFICIAL cap, TESTED cliff.`
- **Evidence:** `research/_addenda/krea-character-art.md` §*New official guidance* item 3; §*Tested findings* A · verification **#54 CONFIRMED**, **K41** (severity **High** — "the app asserts the opposite of the source"), exclusion **#22**
- **Grade:** OFFICIAL (cap) + TESTED (cliff)
- **Risk:** None. **T6** (`test-kit-2026-09.md` §T6) localises the cliff on the fp8 build most students actually run.

### A28 — Krea 2 weights are literal text, not destructive
- **Target:** `KNOWLEDGE ¶KREA FAMILY "Emphasis syntax (word:1.2) is actively destructive"`
- **Change:** REPLACE
- **Current text:** `Emphasis syntax (word:1.2) is actively destructive — restate in different words instead.`
- **Proposed text:** `Emphasis syntax (word:1.2) is not parsed at all on Krea 2 — the stock tokenizer treats it as LITERAL TEXT, so "(red:1.3)" puts the characters into your prompt. Same for {a|b} and []. Weights become real, signed values only inside a community NegPiP patch. Restate in different words instead. OFFICIAL`
- **Evidence:** `research/_addenda/krea-character-art.md` §*Contradicts* #5; §*Tested findings* C · verification GOTCHAS audit (Weights card, severity High), exclusion **#23**
- **Grade:** OFFICIAL (code-level)
- **Risk:** None; "destructive" and "literal" imply the same user action (don't use it) but only one is true, and the literal reading also explains stray punctuation appearing in outputs.

### A29 — Krea 2: unsourced licence thresholds, unsourced enhancer claim, wrong parameter count
- **Target:** `KNOWLEDGE ¶KREA FAMILY "LICENSE is custom (Community License: roughly under $1M revenue"`
- **Change:** REPLACE
- **Current text:** `Krea's from-scratch ~12.9B DiT (Qwen3-VL encoder)` … `LICENSE is custom (Community License: roughly under $1M revenue and under 50 seats, plus content-filter obligations) — fine for teaching, read it before studio use.` … `the pipeline's prompt_enhance is ON by default and RL-trained to INCREASE diversity — turn it off for repeatable character art.`
- **Proposed text:** `Krea's from-scratch 12-billion-parameter DiT (Qwen3-VL encoder)` … `LICENSE is custom (Krea Community License). The one obligation confirmed on the card is content filtering: "deployers are required to implement content filtering measures or equivalent review processes". The revenue and seat thresholds circulating in the community are UNVERIFIED — the licence is a PDF nobody has opened. Read it before studio use.` … `the pipeline's prompt_enhance is ON by default (ComfyUI docs: "The defaults (8 steps, prompt enhancement enabled, no LoRA)…") — turn it off for repeatable character art. The documented risk is not diversity: the enhancer runs through TextGenerate on the same Qwen3-VL-4B and can return an ETHICS REFUSAL that silently becomes your prompt (a "photo of a dog on a kitchen table" produced one). LLM_max_token caps the expansion.`
- **Evidence:** `research/_addenda/krea-character-art.md` §*New official guidance* items 5, 6 (issue #14631, verbatim refusal) · verification **#55, #57 CONFIRMED**, **K39, K40** (severity **High** — "an unsourced licence threshold in a teaching app"), **K44**, exclusions **#24, #26**
- **Grade:** OFFICIAL (default-ON, refusal, parameter count, filter obligation); the removed thresholds and the RL-diversity claim have no source
- **Risk:** Removing the "$1M / 50 seats" numbers leaves students without a bright line. That is correct: the app must not invent a licence threshold. **Next-run action: open `LICENSE.pdf`.**

### A30 — Contest the Krea 2 ReferenceLatent claim
- **Target:** `KNOWLEDGE ¶KREA FAMILY "image reference via VAEEncode→ReferenceLatent"`
- **Change:** DOWNGRADE-LABEL
- **Current text:** `(2) image reference via VAEEncode→ReferenceLatent (does most identity work; text alone is worse) with KEEP-first phrasing;`
- **Proposed text:** `(2) image reference — CONTESTED at source level: Krea2.extra_conds reads only cross_attn (no reference_latents, unlike the QwenImage models), so a latent entry would be SILENTLY DISCARDED and a VAEEncode→ReferenceLatent chain may be dead weight. The identity work is plausibly the Qwen3-VL VISION path's, which is real — and which costs (h/32)·(w/32)+2 tokens against the same 512-position budget, so one 1MP reference (~1026 tokens) already exceeds it on its own. ComfyUI's own style-reference template uses a dedicated INT8 checkpoint plus krea2_style_reference.safetensors, not a latent slot. Use KEEP-first phrasing either way; TESTED both directions, unresolved.`
- **Evidence:** `research/_addenda/krea-character-art.md` §*Contradicts* #1; §*Tested findings* B · verification **K43** (severity High), **#55**
- **Grade:** TESTED vs TESTED (unresolved)
- **Risk:** Telling students a working habit may be useless could cost them a technique that helps. Hence DOWNGRADE, not DELETE. The named test (same seed, same reference, with and without the `ReferenceLatent` node) is in **G2** and is called "the single highest-value in-house test to run" by 1C.

### A31 — Retire "wrong encoder = black images" on klein
- **Target:** `KNOWLEDGE ¶FLUX "ENCODER SPLIT: FLUX.2 dev uses Mistral Small 3.2"`
- **Change:** REPLACE
- **Current text:** `ENCODER SPLIT: FLUX.2 dev uses Mistral Small 3.2, but klein uses QWEN3 (9B needs Qwen3-8B; wrong encoder = black images)`
- **Proposed text:** `ENCODER SPLIT: FLUX.2 dev uses Mistral-Small-3.2-24B, but klein uses SIZE-MATCHED QWEN3 — klein 4B ↔ Qwen3-4B, klein 9B and 9B KV ↔ Qwen3-8B, Base variants the same as their distilled twins. A mismatch does NOT produce a black image: it throws "mat1 and mat2 shapes cannot be multiplied (512x12288 and 7680x3072)" at the sampler, usually reported as "fails right at the step count" (512 = the token cap, 12288 = 3 layers × 4096 for Qwen3-8B, 7680 = 3 × 2560 for what the 4B DiT expects). THE TRAP IS OFFICIAL: ComfyUI's own Flux 2 example page hands you mistral_3_small_flux2_fp8.safetensors and never mentions klein.`
- **Evidence:** `research/flux.md` §*Tested findings* item 7; §*Nothing-found register* first bullet; §*Contradicts* #9 · verification **K32** (severity **High** — "a named diagnostic that is wrong"), exclusion **#13**
- **Grade:** OFFICIAL (code) + USER-VERIFIED (the error string, multiple reporters)
- **Risk:** None. A wrong diagnostic sends students hunting VAE flags for a matmul error.

### A32 — Fix the HOST TRAP line
- **Target:** `KNOWLEDGE ¶GENERAL CONCEPTS "HOST TRAP: several hosted APIs (fal, Segmind)"`
- **Change:** REPLACE
- **Current text:** `HOST TRAP: several hosted APIs (fal, Segmind) expose a negative_prompt field on FLUX.2 and Z-Image that is architecturally DEAD — the pipelines have no string-to-negative path (open diffusers issue #13416) — a silently ignored input, not a feature; treat host /learn pages as SEO, not documentation.`
- **Proposed text:** `HOST TRAP: hosted APIs (fal, Segmind) expose a negative_prompt field that is often silently ignored — treat host /learn pages as SEO, not documentation. The mechanism is precise: Flux2KleinPipeline HARDCODES the negative to "" and has no string argument at all (its docstring says so outright), so on klein a typed negative can never reach the model — that is the real defect in diffusers #13416 (open, zero comments, since 2026-04-04). ZImagePipeline is NOT affected: it accepts a plain-string negative_prompt and encodes it; on Z-Image the field is dead only at guidance_scale == 0, which is the general rule anyway — negatives are a function of GUIDANCE, not of the pipeline.`
- **Evidence:** `research/flux.md` §*Tested findings* item 8; `research/z-image.md` item 13; §*Contradicts* #1 · verification **#27, #36 CONFIRMED**, ruling #3, **K45** (severity High)
- **Grade:** OFFICIAL (code, both pipelines)
- **Risk:** None. Also corrects the 08-28 digest, which extended a klein-only bug to Z-Image.

### A33 — Correct the flagship order-over-prose rule
- **Target:** `KNOWLEDGE ¶GENERAL CONCEPTS "BEST-EVIDENCED CROSS-MODEL RULE (Aug 2026): front-load the subject"`
- **Change:** REPLACE
- **Current text:** `BEST-EVIDENCED CROSS-MODEL RULE (Aug 2026): front-load the subject — word ORDER carries emphasis. A "bag of position-tagged words" (identity + order, no grammar) matched full prose embeddings on quality, attribute binding, spatial relations AND numeracy (MIT/Technion arXiv:2606.03715, image models), and four independent vendor guides converge on order-based emphasis. "Write prose because the encoder is an LLM" is now CONTESTED — what demonstrably matters is which words appear and where.`
- **Proposed text:** `BEST-EVIDENCED CROSS-MODEL RULE: front-load the subject — word ORDER carries emphasis. In a controlled ablation (MIT/Technion arXiv:2606.03715, SD 3 / FLUX.1 Schnell / FLUX.2 Klein-4B), a "bag of position-tagged words" (identity + absolute position, no context) reached 65% non-inferiority against full embeddings' 70-90% — COMING CLOSE TO, not matching — and resolved same-word-set / different-order prompt pairs correctly on every model. SCOPE IT TWO WAYS. By TASK: on GenEval "Single object" the contextless embedding scores 88-100%, but on DrawBench "Text" only 24-37%, and the paper singles out FLUX.2 as still sensitive to losing context — so comma-run slot lists are fine for a single subject, and real relational sentences are needed for spatial/multi-object scenes and for any visible text. By ARCHITECTURE: this is DiT-only. U-Net models collapse without context — SDXL 4%, SD 2.1 0.2% — so never apply it to the SDXL targets. BFL's own guidance independently says the same thing ("Prompt order matters here too… make the subject clear first"). "Write prose because the encoder is an LLM" is CONTESTED, but by task, not universally. TESTED/PAPER`
- **Evidence:** `research/flux.md` §*Tested findings* item 10 (every figure verbatim); §*Validator changes* last two bullets · verification **#28 CONFIRMED** ("the most careful piece of evidence work in the sweep"), **#24 OVERSTATED**, **K28, K29, K30** (all High), exclusions **#16, #17**
- **Grade:** TESTED/PAPER
- **Risk:** The rule gets more complex and therefore harder to teach. That is the honest state: the current line overstates its own citation and leaks a DiT finding onto an SDXL target. **T1** (`test-kit-2026-09.md` §T1 — prose / label block / scrambled, fixed seed, Qwen-Image + Krea 2) is the in-house replication.

### A34 — Drop the uncounted "four vendor guides" claim
- **Target:** `KNOWLEDGE ¶GENERAL CONCEPTS "and four independent vendor guides converge on order-based emphasis"`
- **Change:** DELETE
- **Current text:** `and four independent vendor guides converge on order-based emphasis`
- **Proposed text:** (absorbed into A33's "BFL's own guidance independently says the same thing" — one verified vendor, named)
- **Evidence:** verification **K30** ("Uncounted and unsourced. BFL is one (verified). No list of four exists anywhere in the corpus"), exclusion **#17**
- **Grade:** OFFICIAL for the one that survives
- **Risk:** None; it removes a fabricated tally without weakening the rule.

### A35 — Z-Image Turbo: 8 steps is the NFE count, not the value you type
- **Target:** `KNOWLEDGE ¶Z-IMAGE "Turbo: 8 steps, guidance 0, NO negatives"`
- **Change:** REPLACE
- **Current text:** `Turbo: 8 steps, guidance 0, NO negatives, low seed diversity`
- **Proposed text:** `Turbo: type 9 scheduler steps, not 8 — the vendor's own code comment reads "num_inference_steps=9,  # This actually results in 8 DiT forwards", so the widely-quoted 8 is the NFE count from the zoo table. Guidance 0 (which skips the negative branch entirely), no negatives, low seed diversity. STAFF add a coupled knob: 16-30 steps are possible IF time-shift rises from its default 3 to ~6-12, with no guarantee of improvement — and Turbo is step-distilled, so the Base model is the one to push steps on. OFFICIAL + STAFF`
- **Evidence:** `research/z-image.md` §*Validator changes*; `research/_addenda/staff-claims-2026-09.md` rows 2, 3 · verification **#32 MISLABELLED (too low — upgrade to OFFICIAL)**, **K35**
- **Grade:** OFFICIAL + STAFF
- **Risk:** Low. Rows 2/3 were **not re-fetched** (#60), but they sit on the repo and badge the verifier confirmed for #34/#35.

### A36 — Fix the negatives-active list
- **Target:** `KNOWLEDGE ¶GENERAL CONCEPTS "Negatives active: SDXL (CFG 5-8), Wan (official ZH list)"`
- **Change:** REPLACE
- **Current text:** `Negatives active: SDXL (CFG 5-8), Wan (official ZH list), Qwen-Image (official ZH list), Z-Image Base, FLUX.2 klein Base.`
- **Proposed text:** `Negatives active: SDXL (CFG 5-8), Wan (official ZH list), Wan-Dancer (CFG 5.0, same ZH list), Qwen-Image (official ZH list), Z-Image Base, SCAIL-2 (CFG 5.0 default; the CLI hardcodes the Chinese Wan negative and ComfyUI exposes "negative" as a real conditioning input, which the official template leaves EMPTY — recommend leaving it empty, but stop calling it absent). FLUX.2 klein Base is architecturally live at guidance 4.0 but UNREACHABLE through diffusers, which pins the negative to "".`
- **Evidence:** `research/scail2.md` §*Contradicts* #3; §*Validator changes* #12 · `research/flux.md` item 9 · `research/wan22.md` item 4 · verification **#7, #9, #27 CONFIRMED**, **K46**
- **Grade:** OFFICIAL
- **Risk:** None; it removes an "absent" claim that is wrong in both directions on SCAIL-2.

### A37 — Rewrite the weighting-syntax GOTCHAS card
- **Target:** `GOTCHAS card t:'Weights' m:'(word:1.3) emphasis works everywhere.'`
- **Change:** REPLACE
- **Current text:** `r:'It works on SDXL fine-tunes only — and A1111 vs ComfyUI compute it differently. On Qwen3-encoder models (Krea 2, klein, Z-Image) scaling one token shoves the WHOLE conditioning (destructive past 1.2). {} is NovelAI syntax that collides with ComfyUI wildcards; (...) is stage-direction markup on Wan-Streamer. No portable weighting syntax exists — use order, restatement, and specificity.'`
- **Proposed text:** `r:'It works on SDXL fine-tunes only — and A1111 vs ComfyUI compute it differently. On Qwen3-encoder models (Krea 2, klein, Z-Image) nothing is scaled at all: the stock tokenizer treats "(red:1.3)" as LITERAL TEXT that lands in your prompt. On MiniMax H3 it is architecturally inert (the tokenizer passes disable_weights=True), and {a|b} there is eaten by ComfyUI\'s wildcard expander before the model sees it. {} is also NovelAI syntax; (...) is stage-direction markup on Wan-Streamer. No portable weighting syntax exists — use order, restatement, and specificity. (Community NegPiP nodes re-introduce real signed weights on Krea 2 and Z-Image, at your own risk.)'`
- **Evidence:** `research/_addenda/krea-character-art.md` §*Contradicts* #5; §*Tested findings* C · `research/z-image.md` item 15 · `research/minimax-h3.md` item 3, §*Contradicts* #6 · verification GOTCHAS audit (severity High), exclusion **#23**
- **Grade:** OFFICIAL (all three mechanisms are code-level)
- **Risk:** None. This is 08-28 backlog item #3 with its mechanism corrected — see **F3**.

### A38 — Remove "wrong encoder pairing" from the klein black-frame causes
- **Target:** `GOTCHAS card t:'ComfyUI' m:'Negative-looking output means the model is bad.'`
- **Change:** REPLACE
- **Current text:** `r:'Black frames have specific verified causes: --fp16-vae, wrong encoder pairing, CFG cranked on a distilled model, or the wrong VAE family (Krea 2 uses the QWEN-IMAGE VAE). Check the plumbing before blaming the model.'`
- **Proposed text:** `r:'Black frames have specific verified causes: --fp16-vae (its own help text warns about it), CFG cranked on a distilled model, the wrong VAE family (Krea 2 uses the QWEN-IMAGE VAE), or a Krea 2 prompt past ~512 conditioning positions. A wrong TEXT ENCODER is a different failure: it throws "mat1 and mat2 shapes cannot be multiplied" at the sampler, not a black frame. Check the plumbing before blaming the model.'`
- **Evidence:** `research/flux.md` §*Tested findings* item 7 · `research/_addenda/krea-character-art.md` §*Tested findings* A · verification **K32**, GOTCHAS audit
- **Grade:** OFFICIAL + USER-VERIFIED
- **Risk:** None.

### A39 — Scope the Civitai anime claim
- **Target:** `KNOWLEDGE ¶IMAGE — reach for "Illustrious XL v2 (or NoobAI derivatives) for anime"`
- **Change:** REPLACE
- **Current text:** `Illustrious XL v2 (or NoobAI derivatives) for anime — still king, every top Civitai anime checkpoint builds on it;`
- **Proposed text:** `Illustrious XL v2 and NoobAI derivatives dominate open anime work (community consensus; civitai.com returns an empty body to every automated check we run, so "every top checkpoint builds on it" is not something we can verify — treat it as LORE, not a measurement);`
- **Evidence:** `research/_addenda/staff-claims-2026-09.md` §*Nothing-found register* (civitai.com row) · `research/sdxl.md` §*Surface notes* · verification **K48** ("unfalsifiable as written"), exclusion **#33**
- **Grade:** LORE (scoped)
- **Risk:** None; the recommendation survives, its false precision does not.

### A40 — Keep the ≤75-token advice, drop the mechanism
- **Target:** `TARGETS.sdxl.system "Keep it under ~60 words (≈75 CLIP tokens)"` (≈line 695) **and** `KNOWLEDGE ¶SDXL FAMILY "≤~75 CLIP tokens (Juggernaut creator guidance)"`
- **Change:** REPLACE (both)
- **Current text (target):** `- Keep it under ~60 words (≈75 CLIP tokens). One clause per subject; give each subject its own position/clothing anchor to prevent attribute bleed.`
- **Proposed text (target):** `- Keep it under ~60 words (≈75 CLIP tokens) — the Juggernaut author's own guidance. Past 75 tokens ComfyUI does NOT discard your words: it encodes the prompt in separate 75-token chunks that cannot attend to each other, so cross-chunk relationships are lost. One clause per subject; give each subject its own position/clothing anchor to prevent attribute bleed.`
- **Proposed text (KNOWLEDGE):** append after the existing `(Juggernaut creator guidance)`: ` — author-stated for Juggernaut XL and in the Ragnarok guide; the XI/XII cards state no token guidance, so applying it there is LORE. The mechanism is chunking, not discarding.`
- **Evidence:** `research/_addenda/staff-claims-2026-09.md` row 7 and §*Contradictions surfaced* #1 · verification **#58, #59 CONFIRMED as a correction**, ruling #7, exclusion **#32**
- **Grade:** CREATOR (advice) / TESTED reading (mechanism)
- **Risk:** None. The verifier notes the app currently repeats neither the advice's source nor the false mechanism, "which is the right state" — this makes the advice citable without importing the error.

---

## B — Additions with OFFICIAL/STAFF grade

### B1 — Z-Image's staff token remedy
- **Target:** `KNOWLEDGE ¶Z-IMAGE "512-token default cap SILENTLY truncates"`
- **Change:** REPLACE
- **Current text:** `512-token default cap SILENTLY truncates (~350+ English words is the danger zone; can raise to 1024 locally)`
- **Proposed text:** `512-token default cap SILENTLY truncates the tail (padding="max_length", truncation=True), and the prompt is wrapped in a Qwen chat template with enable_thinking=True, so template tokens count against the budget. STAFF give the exact conversion and the exact remedy: ≈0.75 words per token, so 512 tokens ≈ 380 English words; pass max_sequence_length=1024 in the diffusers call ("we've handled this case in our pipeline"). The 512 is a serving-latency choice, not an architectural limit. STAFF ALSO: the encoder is not swappable — "our diffusion model works exclusively with qwen3-4b".`
- **Evidence:** `research/z-image.md` §*Chinese sources* item 8 (verbatim); `research/_addenda/staff-claims-2026-09.md` row 1 · verification **#34 CONFIRMED** (quote and code block verbatim, org badge renders)
- **Grade:** STAFF
- **Risk:** None; the remedy is a one-line change the vendor itself publishes.

### B2 — What `cfg_normalization` actually is, plus `cfg_truncation`
- **Target:** `KNOWLEDGE ¶Z-IMAGE "cfg_normalization off=stylized/on=realism"`
- **Change:** REPLACE
- **Current text:** `Base (Jan 27, 2026, released): 28-50 steps, CFG 3-5, negatives strongly recommended, cfg_normalization off=stylized/on=realism.`
- **Proposed text:** `Base (released 2026-01-27): 28-50 steps, CFG 3-5, negatives STRONGLY RECOMMENDED (that line and the cfg_normalization line live on the GitHub README only — the HF card drops both). cfg_normalization is a CFG-BURN LIMITER, not a quality switch: it clamps the guided prediction's norm to cfg_normalization × the positive-only prediction's norm, and it is used as a float (True = 1.0; 1.2 is a legal, gentler clamp). That is why the vendor pairs it with realism and turns it off for stylism. A companion parameter the corpus lacked: cfg_truncation (default 1.0, never fires) kills guidance — and therefore the negative — for the late, low-noise steps when lowered. The vendor's own worked Base example is 50 steps, guidance 4, cfg_normalization=False, at height=1280 width=720 (portrait). OFFICIAL`
- **Evidence:** `research/z-image.md` items 2, 3, 4; §*Contradicts* #4, #5, #7 · verification **#31 CONFIRMED**, **#33 OVERSTATED (dimension flipped — corrected here)**, **K36**, exclusion **#27**
- **Grade:** OFFICIAL (code + README)
- **Risk:** `cfg_truncation` should stay an advanced-only field; surfacing it to beginners invites cargo-culting.

### B3 — Z-Image's official multi-subject anti-bleed pattern
- **Target:** `TARGETS.zimage.system "MULTIPLE SAME-CLASS SUBJECTS (couples, groups, N-vs-N scenes)"` (≈line 759, extend the existing bullet)
- **Change:** ADD (append to the bullet)
- **Current text:** ends `…recommend Z-Image BASE (CFG 3-5 + negatives) over Turbo for compositions, and several seeds.`
- **Proposed text:** append ` This mirrors the vendor's own Chinese Base example, which is the official anti-bleed technique: count first (两名), then ONE clause per subject anchored by screen side (左侧/右侧) and closed with "；", each carrying garment + colour + trim + accessory + arm position, then a shared action (两人均…), then a comma-run of capture terms, then a background restatement. Name the risk precisely — MultiBind separates drift (attributes degrade), swap (A gets B's attribute), dominance (one look takes over) and blending (subjects fuse).`
- **Evidence:** `research/z-image.md` §*Chinese sources* item 10 (verbatim official example); §*Tested findings* item 14 · verification (MultiBind claim scoped — the abstract names no models, so do **not** say Z-Image was tested)
- **Grade:** OFFICIAL (the pattern) + TESTED/PAPER (the taxonomy, scoped)
- **Risk:** MultiBind is a multi-*reference* benchmark and does not evaluate prompt-only T2I; the proposed text borrows only its vocabulary, which is the defensible use.

### B4 — Krea 2's nine-slot encoder descriptor
> **RETRACTED (2026-09-13 merge review).** The "nine slots in that order" claim was falsified by the 36-prompt official gallery harvest seven days after it was made (see research/digests/2026-09-10-digest.md and krea-character-art.md). Do not apply as written; the grade below ("OFFICIAL — vendor source code") describes the template string's existence, not the slot-order doctrine built on it.
- **Target:** `TARGETS.krea2.system "ONE prose paragraph, 80-140 words. LEAD WITH THE MEDIUM"` (≈line 802)
- **Change:** REPLACE
- **Current text:** `- ONE prose paragraph, 80-140 words. LEAD WITH THE MEDIUM: "A stylized digital painting with visible brushstrokes of ...", "A minimalist flat-color illustration of ...", "A gouache character study of ...".`
- **Proposed text:** `- ONE prose paragraph, 80-140 words (craft guidance, NOT capacity — the hard ceiling is 512 conditioning positions ≈ 2,850 characters). LEAD WITH THE MEDIUM: "A stylized digital painting with visible brushstrokes of ...". Then follow the encoder's OWN slot order, which it prepends to every prompt as a system instruction: colour → shape → size → texture → quantity → text → spatial relationships → objects → background. That is a better-evidenced ordering rule than any heuristic.`
- **Evidence:** `research/_addenda/krea-character-art.md` §*New official guidance* item 2 (verbatim `prompt_template_encode_prefix`) · verification **#54 CONFIRMED**, TARGETS audit `TARGETS.krea2.system (missing)`
- **Grade:** OFFICIAL (vendor source code)
- **Risk:** None; it is the model's own instruction to itself.

### B5 — Krea 2's reference-image token cost
- **Target:** `KNOWLEDGE ¶KREA FAMILY` (append a sentence after the length-ceiling text from **A27**)
- **Change:** ADD
- **Current text:** none
- **Proposed text:** `A reference image rides the Qwen3-VL VISION path and spends the SAME budget: (h/32)·(w/32)+2 tokens, so one 1-megapixel reference is ~1026 tokens against a ~20-token prompt — it already exceeds the 512-position ceiling on its own. Downscale references hard, or accept that the text is being crowded out. Two 1MP references cannot coexist with a long prompt. Also: ComfyUI strips the SYSTEM turn before the DiT, so the steerable write-points are the user turn and the assistant <think> turn. TESTED`
- **Evidence:** `research/_addenda/krea-character-art.md` §*Tested findings* B (runtime-verified against ComfyUI's `krea2.py`) · verification GOTCHAS audit ("Krea 2 (missing card)", severity High)
- **Grade:** TESTED (author's own "high confidence" tier, checked in-process)
- **Risk:** The token formula is from a third-party probe repo, not the vendor — but it is arithmetic against a verified 512-position cap, and the direction (references are expensive) is robust.

### B6 — The H3 licence block
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "License still excludes local deployment in US/EU/UK/KR"`
- **Change:** REPLACE
- **Current text:** `License still excludes local deployment in US/EU/UK/KR — per MiniMax's own Chinese hub the exclusion is framed as "not yet", not "never"; additionally: written authorization required above $20M revenue, commercial UIs must display "MiniMax H3" attribution, and outputs/weights may NOT train non-H3 models.`
- **Proposed text:** `LICENCE (MiniMax H3 Community License, dated 2026-08-02, Nanonoble Pte. Ltd., Hong Kong SAR law). Excluded Territories = the EU, the UK, the Republic of Korea and the USA — and §V.4 plus Exhibit A item 1 extend that ban to the OUTPUTS, not just the weights. The licence itself frames it as temporary and offers a contact route (§II). §IV.1: written authorisation required above US$20M yearly revenue, via api@minimax.io with the subject line "MiniMax H3 licensing - authorization request". §IV.2 is a SHALL — "prominently display 'MiniMax H3' on the user interface" of a commercial product; the "Powered by MiniMax H3" line (§III.3.a) is only ENCOURAGED. §V.3 + §I.11: outputs and weights may not train any other model, and distillation / intermediate representations / synthetic-data derivation all make a Model Derivative that inherits every term. §III.4 mandates a NOTICE string for non-hosted redistribution. §VI.4: MiniMax claims no rights over your Outputs. The encoder (Qwen3-VL-32B) is Apache-2.0, which does NOT relicense the weights or the outputs. OFFICIAL`
- **Evidence:** `research/minimax-h3.md` §*New official guidance* item 1 (ten verbatim quotations) · verification **#48 CONFIRMED** — "the strongest verified block in the whole sweep"
- **Grade:** OFFICIAL
- **Risk:** None. This is 08-28 backlog #1 discharged. See also **H1**.

### B7 — H3's two new local capabilities
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "2K only via the hosted Regenerate-2K module"` (append after)
- **Change:** ADD
- **Current text:** none
- **Proposed text:** `NEW LOCALLY (ComfyUI core): MiniMaxH3AddGuide anchors a guide image or audio at ANY frame, not just first/last — multi-frame batches crop to valid clip lengths (5, 22, 39…), batches under 5 frames use only the first image, frame_idx accepts negatives (count from the end), audio guides crop to the remaining duration, and the node is chainable. Plus per-token latent noise masks for video AND audio: "a value of 0 preserves the corresponding latent region, while 1 regenerates it. Video masks snap to the model's 2x2 latent patch grid, and audio masks to whole latent frames." That is local inpainting, object removal and clip extension without a hosted tier. OFFICIAL`
- **Evidence:** `research/minimax-h3.md` §*New official guidance* item 7 · verification **#53 CONFIRMED verbatim**
- **Grade:** OFFICIAL
- **Risk:** None. Note #51: do not use "only four nodes in `nodes_minimax_h3.py`" as evidence about H3's capabilities — `MiniMaxH3AddGuide` is a documented fifth node that lives elsewhere.

### B8 — H3's reference-tag mechanics, upgraded from LORE to OFFICIAL
- **Target:** `KNOWLEDGE ¶MINIMAX H3 "Reference TAG MECHANICS (community-documented)"`
- **Change:** REPLACE
- **Current text:** `Reference TAG MECHANICS (community-documented): tags number by ARRIVAL ORDER, not slot — gaps close up (media in slots 2 and 5 becomes <Picture 1> and <Picture 2>); a video's soundtrack, if used, splits into its OWN audio reference (numbered just before standalone audio) and counts against BOTH the 12-file cap and the 3-audio budget`
- **Proposed text:** `Reference TAG MECHANICS (now OFFICIAL, read off the node source): "References enter the presentation in fixed order: images, then videos (each soundtrack's <Audio j> label right before its <Video k>), then standalone audio. Ordinals are 1-based per type." Numbering follows SUPPLIED assets, not slots — empty slots are skipped and never counted, so filling slot 3 while slot 2 is empty still yields <Picture 2>. Equal numbers across types imply NO pairing. A reference video's soundtrack is a separate input paired by index, and it takes its own <Audio j> ordinal EMITTED BEFORE its <Video k> — so writing a standalone voice reference as <Audio 1> when a video soundtrack is attached silently points at the wrong asset, which is the single most likely Ref2VA authoring error. Audio never enters Qwen; Qwen sees a reference video at 2 fps with <T.T seconds> timestamps per 2-frame block. ref_image_size "max" uses a 2048px short edge for identity fidelity but reference tokens ride through every sampling step, so it can be several times slower — and references are never upscaled, so "max" buys nothing from a soft source. OFFICIAL`
- **Evidence:** `research/minimax-h3.md` §*Reference-tag mechanics* (node + tokenizer docstrings verbatim) · verification **#50, #52 CONFIRMED**, GOTCHAS audit (H3 tag card: "upgrade the grade")
- **Grade:** OFFICIAL (was LORE)
- **Risk:** None; and it removes the "12-file cap" phrasing corrected in **A15**.

### B9 — Wan's EN/ZH rewriter asymmetry, stated honestly
- **Target:** `KNOWLEDGE ¶WAN 2.2 "Official extender (--use_prompt_extend, Qwen-based, separate native ZH and EN rewriters)"`
- **Change:** REPLACE
- **Current text:** `Official extender (--use_prompt_extend, Qwen-based, separate native ZH and EN rewriters) bans literary mood padding.`
- **Proposed text:** `Official extender (--use_prompt_extend, Qwen-based; constants live in wan/utils/system_prompt.py, NOT prompt_extend.py) bans literary mood padding — rule 3 verbatim: 不要输出关于氛围、感觉等文学描写. Its default target language is CHINESE (tar_lang="zh"), so running it on an English prompt without --prompt_extend_target_lang rewrites your prompt into Chinese. The two rewriters differ: the EN one caps aesthetic tokens at 不超过4种 while the ZH one says 部分 with no cap — but the vendor's own English exemplar 2 carries TEN, so treat 4 as the instruction and not as a measured band. The EN system prompt is itself written in Chinese and its exemplar leaks untranslated text ("The俯拍close-up"), which is direct evidence Chinese is Wan's primary prompt language. ZH 镜头尺寸 has no 极端特写; EN has "Extreme close-up shot". OFFICIAL`
- **Evidence:** `research/wan22.md` item 1(a)–(d) · verification **#38, #39, #41 CONFIRMED**, **#40 UNSUPPORTED** (the exemplar tally — handled here by stating the instruction, not a band), exclusion **#7**
- **Grade:** OFFICIAL
- **Risk:** None, provided the "4" is never made a validator error — see **D6**. **T4** (`test-kit-2026-09.md` §T4) tests whether the cap is a model limit or a style choice.

### B10 — Illustrious control tokens
- **Target:** `KNOWLEDGE ¶SDXL FAMILY` (append after the anime sentence replaced in **A24**)
- **Change:** ADD
- **Current text:** none — the verifier calls this "the largest missing vocabulary"
- **Proposed text:** `ILLUSTRIOUS CONTROL TOKENS (v3.0-epsilon and v3.5-vpred; the author says v3.0-vpred "may not work well" with them): six explicitly tagged axes — contrast (low/medium/high/very high contrast), brightness (dark/normal brightness/bright/very bright), sharpness (blurry/slightly sharp/sharp/very sharp), dynamic colors (static/medium dynamic/high dynamic/very dynamic colors), colorfulness (monotonic color/medium/high/very high colorfulness), saturation (muted/average/vibrant/very vibrant colors). They exist because a 4.4M-image dataset audit found "variant colored" images under 10%, so v3.5-vpred does not produce vibrant colour on its own — the colour moved into the tokens. Author's warning: "dark" is CONTAMINATED, prefer "black theme". CREATOR`
- **Evidence:** `research/sdxl.md` §*New official guidance* (Illustrious dev blog, verbatim); §*Contradicts* #7 · verification **K26**
- **Grade:** CREATOR (author's words) via a `[LORE]` reprint channel — the Onoma-hosted original is client-rendered and unreadable
- **Risk:** The channel is a third-party reprint. Mitigated by grading it CREATOR-via-reprint in the bank text if the maintainer prefers; the vocabulary itself is unambiguous and testable in seconds.

### B11 — Wan-Animate-2's third prompt field
- **Target:** `KNOWLEDGE ¶WAN 2.2 "OFFICIAL CHINESE two-field dialect — 人物外观描述： and 背景描述："`
- **Change:** REPLACE
- **Current text:** `character animation with an OFFICIAL CHINESE two-field dialect — 人物外观描述： and 背景描述： — and explicitly NO action description (motion comes from the driving input).`
- **Proposed text:** `character animation with an OFFICIAL CHINESE two-field dialect — 人物外观描述： and 背景描述： — captioning the REFERENCE IMAGE ONLY. Four hard rules from the vendor's own captioning meta-prompt: Chinese; NO action or behaviour description (不描述动作行为, because the driving video supplies all motion); no subjective evaluation or emotional inference (忽略主观评价和情绪推测); and NO extra punctuation or symbols (不要输出额外的符号) — the two labels are the only structure allowed. 人物外观描述 is used for non-human subjects too (the README's own example is a cat). A THIRD field exists in the DiffSynth path: animate2_prompt_ref captions the DRIVING video ("视频中的人在做动作，背景静止"). CFG is 0.0-1.0 across the official configs, so do not teach Animate-2 negatives as live. Wan-Animate-2-Lite has NO open weights. OFFICIAL`
- **Evidence:** `research/wan22.md` item 3; §*Contradicts* #7 · verification GOTCHAS audit (Wan 3.0 card)
- **Grade:** OFFICIAL (dialect) / SPECULATION (negatives inert at CFG 0–1)
- **Risk:** None; it closes a gap no corpus position covered.

### B12 — LTX multi-shot: the four per-cut requirements
- **Target:** `TARGETS.ltx.system "No cuts or timestamps for 2.3; simple named cuts are allowed only if the user asked and is on 2.5."` (≈line 594)
- **Change:** REPLACE
- **Current text:** `- No cuts or timestamps for 2.3; simple named cuts are allowed only if the user asked and is on 2.5.`
- **Proposed text:** `- No cuts or timestamps for 2.3. On 2.5, cuts are legal only when the user asked AND you NAME the transition in prose ("A hard cut transitions to…", "The view cuts to…", "A match cut connects…", "The image dissolves into…"). At every cut do all four: name the transition; re-establish shot scale, angle, who is in frame and any lighting change; re-identify recurring subjects with the same visual identifiers; and state audio continuity ("the piano score continues across the cut" / "the dialogue drops; only wind remains"). Prefer 2-4 shots; give each a clear job (establish → detail → reaction). Never emit a numbered shot list or bare sluglines unless the cut is also named in prose.`
- **Evidence:** `research/ltx23.md` item 3 (whole Multi-Shot section verbatim) · verification **#13 CONFIRMED byte-for-byte**, TARGETS audit ("CONFIRMED and well-scoped. Add the four official per-cut requirements and the 2–4-shot band")
- **Grade:** OFFICIAL
- **Risk:** None; the existing rule is right and merely under-specified.

---

## C — New targets / dialects

### C1 — `wanAnimate2` target
- **Target:** `TARGETS.wanAnimate2` (new key) + `WF_MAP` entry
- **Change:** ADD
- **Current text:** none
- **Proposed text (system prompt core):** `You caption a REFERENCE IMAGE for Wan2.2-Animate-2. Motion comes entirely from the driving video; a motion verb here competes with it. Output exactly two Chinese labelled fields on one line, in this order and with nothing else: 人物外观描述：<appearance, clothing, materials, colours, accessories — no actions, no emotions, no camera, no style> 背景描述：<setting, surfaces, lighting quality — objective only>. Rules from the vendor's own captioning meta-prompt: Chinese only; 不描述动作行为; 忽略主观评价和情绪推测; 不要输出额外的符号. Use 人物外观描述 for non-human subjects too. No negative prompt (official CFG is 0.0-1.0). Keep the caption well under 512 UMT5 tokens. Output ONLY the two fields.` Example: `人物外观描述：一名长黑发女性，穿着白色半透明蕾丝长袖上衣。背景描述：背景为现代室内空间，墙面和柜体以浅灰色为主。`
- **Evidence:** `research/wan22.md` item 3; §*Few-shot gold* Pair 6; §*Validator changes* #7
- **Grade:** OFFICIAL
- **Risk:** The two-field format is a *recommendation the code does not enforce* — the Base yaml's own placeholder is English (`input_prompts: ['static background.']`). Ship it as the taught form with that caveat in `wfNotes`.

### C2 — `wanDancer` target
- **Target:** `TARGETS.wanDancer` (new key)
- **Change:** ADD
- **Current text:** none
- **Proposed text (system prompt core):** `You fill a fixed Wan-Dancer caption schema. It is NOT prose and NOT the Wan cinematic formula. Global stage: 一个人正在跳舞，舞蹈种类是{genre}。 Local stage: 一个人正在跳舞，舞蹈种类是{genre},图像清晰程度{高},人物动作平均幅度{中等},人物动作最大幅度{中等}。 genre is one of 古典舞 / 街舞 / 韩舞 / 拉丁舞 / 踢踏舞 — 韩舞 is the token for K-pop, not a transliteration. Only 高 (clarity) and 中等 (both amplitudes) are attested in the shipped files; other rungs are unverified. Add NOTHING else — no cinematography, lighting, camera or style words. Do not type 帧率是… ; the script appends it from the music. CFG is 5.0, so the standard Chinese Wan negative list IS live. Output ONLY the caption.`
- **Evidence:** `research/wan22.md` item 4; §*Few-shot gold* Pair 5; §*Validator changes* #8
- **Grade:** OFFICIAL
- **Risk:** The global stage asserts `world_size == 8` (8 GPUs), so most students cannot run it — say so in `wfNotes`, and treat this target as vocabulary teaching rather than an export path. The 低/高 amplitude rungs are **not attested** (see **G12**).

### C3 — Bernini-R: `KNOWLEDGE` entry + target candidate
- **Target:** `KNOWLEDGE ¶VIDEO MODELS` (new paragraph) + `TARGETS.berniniR` (new key)
- **Change:** ADD
- **Current text:** none — Bernini-R appears nowhere in `research/` before this run and nowhere in the app
- **Proposed text (KNOWLEDGE):** `BERNINI-R (ByteDance, Apache 2.0, Wan 2.2 backbone; ComfyUI CORE nodes + official Comfy-Org repackage + official tutorial). A RENDERER-ONLY model for in-context image and video conditioning — six task types: t2v, v2v (restyling), rv2v (relight / subject insertion), r2v, img, ads2v (upstream adds t2i, i2i, mv2v; the vendor's rule is that edits needing to CHANGE the subject's motion take mv2v). Because it is Wan 2.2 plus a trained renderer with the same UMT5 encoder, the GENERATION dialect is Wan's: its own official t2v test prompt opens "Day time, side lighting, medium shot, center composition." — the Wan 电影美学 axis order emitted verbatim by a non-Alibaba vendor. What is NEW is the conditioning grammar: reference images are addressed POSITIONALLY as image0, image1, … in the prompt (source_image instead, for plain image editing), and the EDITING dialect inverts our brevity rule — the official rv2v example spends over half its words enumerating what must NOT change (pose, fit behaviour, camera framing, lighting, background, pants, hair, skin, shadows, motion) and closes "all other scene elements remain unchanged". Its "best quality" path --use_pe is another hidden cloud rewriter (OpenAI-compatible endpoint); ComfyUI does not include it. The official ComfyUI recipe ships the lightx2v cfg-step-distill LoRA, i.e. a distilled low-CFG configuration. Template declares ≈38.8 GB; a third-party "runs in 24GB with fp8" claim is LORE and untested. OFFICIAL`
- **Evidence:** `research/new-models.md` §*Bernini-R*; §*What Bernini-R's dialect would need* · verification **K49** — **NOT RE-FETCHED by the verifier; verify the tutorial, the repackage and PR #14216 before folding**
- **Grade:** OFFICIAL (pending re-verification)
- **Risk:** The single largest unverified addition in this document. Two facts are third-hand: PR #14216's title/author came from a search listing, and the 24 GB claim is a wrapper README. Verify the docs.comfy.org tutorial page first; everything else in the paragraph rests on it plus the two official test-case JSONs.

### C4 — Split the LTX target by version
- **Target:** `TARGETS.ltx` → `TARGETS.ltx23` + `TARGETS.ltx25` (or a version switch inside one key)
- **Change:** ADD
- **Current text:** one `ltx` key covering "LTX 2.3/2.5" with a single 150-word cap and a single paragraph rule
- **Proposed text:** Split so that **2.3** keeps a word band (40–150 soft, 200 hard from the README) and the strict no-cuts rule; **2.5** gets **4–8 sentences** single-shot, up to ~16 for multishot/screenplay, the named-cut requirements from **B12**, the enhancer-state note from **A6**, and permission for the screenplay form (`Style:` prefix, scene headers, character cues, quoted dialogue) when the scene is dialogue-driven. Route **Dub-It** intents to **2.3** (the adapter is 2.3-only) using the official slot template `[Speaker] is speaking [Language/Accent], saying: "[Dialogue]"`, with its four hard rules: supply the full target-language text (it does not translate), native script, ONE speaker, and syllable-length parity with the original line.
- **Evidence:** `research/ltx23.md` items 1, 3, 14; §*Validator changes* V4, V6, V14; §*Few-shot gold* Pairs 5–7 · verification **#12 CONFIRMED**, **#14 OVERSTATED** (the slugline tally — so cite the guide's own "Longer / Screenplay-Style" section, not a count of samples), TARGETS audit
- **Grade:** OFFICIAL
- **Risk:** Two targets is more UI surface. Alternative: one target plus a 2.3/2.5 selector, which is cheaper and matches the app's existing variant-routing pattern. Do **not** justify the screenplay permission with "two of three samples use sluglines" — there are two samples and one qualifies (#14).

---

## D — Validator & export changes

### D1 — `sdxlAnime` must gate on a family selector
- **Target:** `validator sdxlAnime score_9 / source_* checks in PromptStudio.html near line 1256-1258`
- **Change:** REPLACE
- **Current text:** `if (!/score_9/.test(t)) issues.push('missing Pony score ladder (score_9, score_8_up...)');` / `if (!/source_(anime|furry|pony|cartoon)/.test(t)) issues.push('missing source_* tag');`
- **Proposed text:** Gate every rule on a `family` selector — `pony6 | ponyV7 | noobaiEps | noobaiVpred | illustrious | illustriousVpred | animagine`. **pony6:** require the chain to begin `score_9, score_8_up, score_7_up` (warn if `score_9` appears without at least the next two, per the card's "much weaker effect"), prefer all six rungs, require exactly one `source_*`, allow one `rating_*`, and **reject** `masterpiece|best quality|hd|8k|amazing quality|high score|great score`. **noobaiEps/Vpred:** require the prefix `masterpiece, best quality, newest, absurdres, highres, safe,` and treat `newest|recent|mid|early|old` as PERIOD tags, never quality words. **animagine:** require the count tag first, and **error if `masterpiece|high score|great score|absurdres` appears before the last third** — Animagine places them at the END. **illustrious:** require no prefix at all; offer `masterpiece, best quality, amazing quality` only as a `[LORE]` suggestion. **ponyV7:** apply NO SDXL rule (it is AuraFlow) — no CLIP-skip field, no 77-token warning, `score_*` permitted but never required, enforce the order `special tags → factual description → stylistic description → content tags`, 768–1536 px, ≥30 steps, and reject text-in-image requests.
- **Evidence:** `research/sdxl.md` §*Validator changes* (per-family blocks); §*What our current `sdxlAnime` export and `SDXL_NEG` get wrong* #1, #2 · verification TARGETS audit (validator row 1256-1258: "Wrong for four of five families")
- **Grade:** OFFICIAL / CREATOR per card
- **Risk:** The selector adds a required user choice. Mitigated by defaulting to whatever the exported workflow's checkpoint filename matches, and by "if the family is unknown, refuse to emit a scheduler at all" (**D4**).

### D2 — Remove `sdxlAnime`'s "15-35 tags" and scope the prose ban
- **Target:** `validator sdxlAnime tag-count and prose checks in PromptStudio.html near line 1261-1262` **and** `TARGETS.sdxlAnime.system "Output canonical English Danbooru-style tags"` (≈line 714) **and** `"15-35 tags total"` (≈line 717)
- **Change:** REPLACE
- **Current text:** `if ((pos.match(/,/g) || []).length < 8) issues.push('too few tags — this dialect wants 15-35 canonical Danbooru tags');` / `if (/\b\w+\s+\w+\s+\w+\s+\w+\s+\w+\s+\w+\b[^,]*[.!]/.test(pos)) issues.push('contains prose sentences — tags only');`
- **Proposed text:** Delete the 15-35 band (**no anime card states a tag count**). Replace it with per-family ORDER templates: Animagine `1girl/1boy/1other, character name, series, rating, everything else, quality tags at the END`; NoobAI `<1girl/1boy/…>, <character>, <series>, <artists>, <special tags>, <general tags>, <other tags>`; Illustrious `rating tags → subject → artist tags → absurdres/highres at the end`. Scope the prose check to **Animagine only** ("natural language input may not be effective") and **remove it for Pony V6**, whose card says the model "is trained on combination of natural language prompts and tags and is capable of understanding both, so describing intended result using normal language works in most cases."
- **Evidence:** `research/sdxl.md` §*Validator changes*; gap list #7 · verification TARGETS audit rows for `15-35 tags` (**UNSUPPORTED**) and `NO prose sentences` (**contradicted by Pony V6's own card**), exclusions **#30, #31**
- **Grade:** OFFICIAL / CREATOR
- **Risk:** Losing a length heuristic may let students under-specify. The per-family order templates are stricter and quotable.

### D3 — Per-family negatives instead of one hardcoded list
- **Target:** `TARGETS.sdxlAnime.system "Negative prompt: lowres, bad anatomy, bad hands, text, error"` (≈line 719)
- **Change:** REPLACE
- **Current text:** `Negative prompt: lowres, bad anatomy, bad hands, text, error, missing fingers, extra digit, fewer digits, cropped, worst quality, low quality, signature, watermark`
- **Proposed text:** Emit the family's own list. **NoobAI (eps and v-pred):** `nsfw, worst quality, old, early, low quality, lowres, signature, username, logo, bad hands, mutated hands, mammal, anthro, furry, ambiguous form, feral, semi-anthro`. **Animagine 4.0:** `lowres, bad anatomy, bad hands, text, error, missing finger, extra digits, fewer digits, cropped, worst quality, low quality, low score, bad score, average score, signature, watermark, username, blurry`. **Pony V6:** emit NO negative by default and say why ("designed to not need negative prompts in most cases"); warn if the user's negative exceeds ~12 tokens. **Illustrious:** offer the third-party list explicitly labelled `[LORE]`.
- **Evidence:** `research/sdxl.md` §*Verbatim blocks*; gap list #3 · verification TARGETS audit (`sdxlAnime` negative row: "Nobody's official list — but narrower than 1B says")
- **Grade:** OFFICIAL / CREATOR
- **Risk:** The verifier found the current list is **close to Animagine's**, and the two tokens 1B calls wrong (`missing fingers`, `extra digit`) **match Animagine's widget metadata exactly** while §3 of the same card uses the singular/plural the other way — two variants coexist on one card. Use the §3 form above and note the variance; do not claim the app's current list is simply wrong, only that it is not NoobAI's and contradicts Pony V6.

### D4 — v-pred plumbing in the workflow note, and split the SDXL template
- **Target:** `wfNotes/sdxl` (`PromptStudio.html:2157`) **and** `WF_MAP` (`PromptStudio.html:1713`)
- **Change:** REPLACE (note) + ADD (template split)
- **Current text (note):** `⚠ Sampler is set to dpmpp_2m/karras (photoreal default) — NoobAI v-pred checkpoints ban Karras; adjust per your checkpoint card.`
- **Current text (map):** `const WF_MAP = { wan:'wan5b', wanI2V:'wan5b', ltx:'ltx', sdxl:'sdxl', sdxlAnime:'sdxl', …`
- **Proposed text (note):** `⚠ Sampler is dpmpp_2m/karras (photoreal default). V-PRED CHECKPOINTS NEED FOUR CHANGES TOGETHER, not just a scheduler swap: (1) sampler Euler — or DDIM — and never a Karras schedule; (2) prediction_type = v_prediction; (3) rescale_betas_zero_snr = true; (4) a CFG-Rescale / dynamic-CFG node at ≈0.2. Also drop CFG to 4-5. This applies to NoobAI v-pred AND to Illustrious v3.0-vpred / v3.5-vpred. Conversely RealVisXL REQUIRES a Karras variant — the two must never share a preset. Pony V6 additionally requires CLIP-skip 2.`
- **Proposed text (map):** give `sdxlAnime` its own template key so one graph is not asked to carry a Karras requirement and a Karras ban, a baked-in VAE and a CLIP-skip-2 loader at once.
- **Evidence:** `research/sdxl.md` gap list #5, #6, #8; §*Validator changes* (NoobAI v-pred block, cross-family guards) · verification TARGETS audit rows for `WF` note 2157 ("Under-specified on five counts") and `WF_MAP` 1713
- **Grade:** OFFICIAL / CREATOR
- **Risk:** A second template is real maintenance. The minimum viable version is the note text plus a family switch on the existing template.

### D5 — Krea 2: hard token error, reference budget, expander interlock
- **Target:** `validator krea2 length checks in PromptStudio.html near line 1306-1308`
- **Change:** REPLACE
- **Current text:** `const w = enWords(t); if (w < 55) issues.push('too short (' + w + ' words, want 80-140 of concrete description)'); if (w > 170) issues.push('too long (' + w + ' words, want 80-140)');`
- **Proposed text:** Keep the 80–140 craft band as a **warning**, and add a separate **error** tier: >512 conditioning positions is an error, not a warning — "past the reference cap; Krea's own encoder truncates here and ComfyUI does not, so outputs go black or noisy with no exception." With no tokenizer available, error above **2,500 characters**. Add an **expander interlock**: if `prompt_enhance` is on AND the prompt already exceeds ~1,400 characters, warn that the expansion can push the total over the cliff, and recommend turning it off or lowering `LLM_max_token`. Add a **reference-image budget**: subtract `(h/32)·(w/32)+2` per reference before validating the text, and warn hard that a single 1 MP reference (~1026 tokens) already exceeds the 512 budget on its own.
- **Evidence:** `research/_addenda/krea-character-art.md` §*Validator changes* (first three bullets) · verification **#54 CONFIRMED**, **K41**, GOTCHAS audit ("Krea 2 (missing card)", severity High)
- **Grade:** OFFICIAL (cap) + TESTED (cliff, reference cost)
- **Risk:** Erroring at 512 could block prompts that work fine in practice — the observed cliff is at (576, 640]. Mitigated by wording the error as "undefined behaviour above the reference cap" rather than "this will fail". **T6** localises it.

### D6 — Wan: aesthetic-token budget by language, and angle-vs-move exclusion
- **Target:** `validator wan checks in PromptStudio.html near line 1191-1194`
- **Change:** ADD
- **Current text:** the `camMoves > 2` and `cinematic` checks (keep); the `orbit` check (deleted in **A3**)
- **Proposed text:** Add **advisory** (never error): warn above ~4 front-loaded comma-separated aesthetic tokens in an **English** Wan T2V prompt, allow up to ~10 in Chinese, and word the warning as *"the EN rewriter is instructed 不超过4种 while the ZH one has no cap — though the vendor's own English exemplar carries ten, so this is guidance, not a measured limit."* Add a **mutual-exclusion** check: if the prompt contains a 运镜 term (`镜头.*(推|拉|移|摇|绕)|环绕运镜|复合运镜|push in|pull back|pan|tilt|dolly|truck|arc shot`) **and** a 拍摄角度 token from the closed set (`过肩镜头角度拍摄|低角度拍摄|高角度拍摄|倾斜角度拍摄|航拍|俯视角度拍摄`), warn — the vendor's own rewriter is instructed to suppress the latter. Add a **mood-prose** warning for `氛围|感觉|充满.*感|张力` with rule 3 quoted as the reason.
- **Evidence:** `research/wan22.md` §*Validator changes* #1, #2, #4 · verification **#39 CONFIRMED**, **#40 UNSUPPORTED**, exclusion **#7**
- **Grade:** OFFICIAL (the instructions) — advisory only, per the verifier
- **Risk:** Making the 4-token band an error would build a rule on a band the vendor's own exemplar breaks. **Advisory only.** **T4** settles it.

### D7 — LTX-2.5 validator rules
- **Target:** `validator ltx checks in PromptStudio.html near line 1201-1210`
- **Change:** REPLACE + ADD
- **Current text:** `if (w > 150 && w <= 200) issues.push('over the 150-word enhancer ceiling (' + w + ' words) — OK for 2.5, trim for 2.3'); if (w > 200) issues.push('too long (' + w + ' words) for any LTX version');`
- **Proposed text:** Version-split the length rule (2.3: 40–150 soft, 200 hard; 2.5: **4–8 sentences** single-shot, up to ~16 multishot, warn only when a sentence carries no concrete visual or audio detail). Add: **frame grid** — warn unless `num_frames % 8 == 1` and `width % 32 == 0 && height % 32 == 0`, and note that Dub-It and Retake **silently snap** rather than erroring. **Enhancer state** — if the target is LTX-2.5 in ComfyUI, always surface "the official T2V template ships `prompt_enhance = true`". **Named cuts** — if the prompt implies more than one shot, require a named transition plus a re-identified subject plus an audio-continuity clause per cut; warn above 4 shots; conversely warn on any cut language when a single continuous take was asked for. **Static camera** — if the intent contains "static", "locked-off", "tripod" or "no camera movement", warn that prose is reported to fail in 2.5 I2V (a user reports ~2 successes in ~50 attempts, with the failing prompt published and already following our own advice) and route to the closed-set `Static` camera-control LoRA. **Encoder pairing (FLUX target, same pattern):** klein 4B → Qwen3-4B, klein 9B/9B KV → Qwen3-8B, dev → Mistral-Small-3.2-24B, with the `mat1 and mat2` diagnostic quoted.
- **Evidence:** `research/ltx23.md` §*Validator changes* V6, V9–V13; §*Tested findings* (static-camera report) · `research/flux.md` §*Validator changes* (encoder pairing) · verification **#12, #19**, TARGETS audit
- **Grade:** OFFICIAL, except the static-camera item which is USER-VERIFIED
- **Risk:** The static-camera warning rests on one reporter with no seeds. It is graded USER-VERIFIED and phrased as a report, not a law.

### D8 — H3: hard length error, frame grid, resolution cap, tag arithmetic
- **Target:** `validator minimax / minimaxref checks in PromptStudio.html near line 1211-1236`
- **Change:** ADD
- **Current text:** the existing field/shot/arrow checks (keep)
- **Proposed text:** Add: **frame count** must satisfy `n % 17 == 5`, snapping **up**; warn outside 124–362 (5.17 s to 15.08 s, "trained range… longer is untested"); note the training recipe's floor is 107. **Resolution** multiples of 32, short edge 768, area ≤ 1,032,192 px — reject 1376×768 explicitly with the docs' own explanation ("Skip the 1.0 Megapixel step"), default 1344×768. **Length guard** — warn on a single unbroken text run long enough to risk the hard error `MiniMax H3 text segment exceeds the supported prompt length.`; no token number is published, so phrase it as a "split into shots" nudge (`[SPECULATION]` on where the boundary is). **Ref2VA tag arithmetic** — compute expected labels from the attached assets in connection order (images 1..n; then for each video, its soundtrack takes the next `<Audio>` ordinal BEFORE that video's `<Video>` ordinal; standalone audio last), then require every supplied label to appear and no unsupplied label to appear; `<Audio>` may reach 6; local caps 9/3/3/3, with the 12-total as an **advisory**, not a block. **Weights** — `(word:1.2)` on an H3 prompt is an **error** (`disable_weights=True`, and the parens can render on-screen); `{a|b}` is a **warning** (the wildcard expander eats it). `embedding:<name>` is a **recognised token**, not an error. **Field order** for Ref2VA is the six machine-checkable field names, with the style sentence required as the first line of `detailed_description` rather than as its own field. **No timestamp on `[Shot 1]`**.
- **Evidence:** `research/minimax-h3.md` §*Validator changes* 1–12 · verification **#50, #52 CONFIRMED**, exclusions **#18, #19, #21**
- **Grade:** OFFICIAL, except the length boundary (SPECULATION) and the 12-total (LORE, advisory)
- **Risk:** None if the 12-total stays advisory and the length guard stays a nudge.

### D9 — `SDXL_NEG` and the missing CLIP-skip field
- **Target:** `SDXL_NEG (PromptStudio.html:526)` **and** `validator sdxlAnime` (add a CLIP-skip requirement)
- **Change:** REPLACE + ADD
- **Current text:** `const SDXL_NEG = "lowres, worst quality, low quality, jpeg artifacts, blurry, deformed, disfigured, watermark, text, signature, cropped, bad anatomy, extra limbs, mutated hands";`
- **Proposed text:** Route by checkpoint family instead of shipping one invented consensus. **RealVisXL:** its own published list, `bad hands, bad anatomy, ugly, deformed, (face asymmetry, eyes asymmetry, deformed eyes, deformed mouth, open mouth)`. **Juggernaut XI / XIII Ragnarok:** emit **no** standing negative — the author's own instruction is "Start with no negative, and add afterwards the Stuff you don´t wanna see in that image", with NSFW tokens added when SFW output is required. **Unknown family:** keep a short generic list and say it is generic. Separately: add a **CLIP-skip field** to the `sdxlAnime` export and **require 2 for Pony V6** — its one hard card requirement ("otherwise you will be getting low quality blobs") is currently unenforceable.
- **Evidence:** `research/sdxl.md` gap list #4, #5; §*Verbatim blocks* (RealVis, Ragnarok) · `research/_addenda/staff-claims-2026-09.md` row 8 · verification **#58 CONFIRMED**, TARGETS audit (`SDXL_NEG` row: "Invents a consensus")
- **Grade:** CREATOR (both lists, author-stated)
- **Risk:** Shipping an empty negative for Juggernaut will look like a bug to students used to mega-lists. Mitigate with a one-line explanation in `wfNotes`.

### D10 — Z-Image: quantified token budget, Turbo/Base branch
- **Target:** `validator zimage length checks in PromptStudio.html near line 1280-1287` **and** `TARGETS.zimage.system "NO negative prompt (Turbo has no CFG)"` (≈line 758)
- **Change:** REPLACE (both)
- **Current text (validator):** `if (w > 330) issues.push('too long (' + w + ' words) — the 512-token cap truncates silently past ~350');`
- **Current text (target):** `- NO negative prompt (Turbo has no CFG). To get variation between runs, vary composition/lens/layout wording — Turbo's seed diversity is officially low.`
- **Proposed text (validator):** Warn above **480 tokens** (default 512). With no tokenizer, use the staff conversion of 0.75 words/token: warn above **360 English words** or ~**450 Han characters**; hard-warn above 760 words even with `max_sequence_length=1024` set. Offer the exact remedy in the warning text.
- **Proposed text (target):** `- VARIANT GATE. Turbo: type 9 scheduler steps (= 8 DiT forwards), guidance 0.0, NO negative prompt — guidance 0 skips the negative branch entirely. To get variation, vary composition/lens/layout wording; Turbo's seed diversity is officially "Low". Base: 28-50 steps, CFG 3.0-5.0, negatives STRONGLY RECOMMENDED and officially "responsive" — emit a short targeted negative (e.g. 多余的人物, 面部变形, 手部畸形, 文字, 水印), never a mega-list; cfg_normalization is a CFG-burn limiter (False for stylism, True for realism).`
- **Evidence:** `research/z-image.md` §*Validator changes*; items 1, 2, 7 · verification **#31, #34 CONFIRMED**, **#32**, TARGETS audit (`TARGETS.zimage.system` rows)
- **Grade:** OFFICIAL + STAFF
- **Risk:** None. The target currently conflates Turbo and Base while the app's own `KNOWLEDGE` distinguishes them.

---

## E — Tutor ops lines (from `comfyui-ops-2026-09` "Proposed tutor knowledge lines", filtered by verification)

The source file proposes 20 lines. Filtered out: its `api.github.com` tooling absolute (falsified as an
absolute, verification Method §*The tooling claim four agents made is wrong*) and line 20's cross-GPU
determinism claim, which its own register grades `[SYNTHESIS]` — carried here with that label. The
remainder are OFFICIAL from docs.comfy.org or ComfyUI source and are safe to fold.

### E1 — Version anchor
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Version: Settings→About."` (extend that sentence)
- **Change:** REPLACE
- **Current text:** `Version: Settings→About.`
- **Proposed text:** `Version: Settings→About shows TWO versions (core and frontend). As of Sept 3 2026: core v0.34.3 (Sept 2), frontend package 1.49.6, workflow-templates 0.11.48, embedded-docs 0.5.10, minimum PyTorch 2.7. git pull alone leaves you split-versioned — always pip install -r requirements.txt after, or you get "Frontend or Templates Package Not Updated". OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Version anchors*; proposed line 1; error table #21
- **Grade:** OFFICIAL
- **Risk:** Version numbers date fast; keep the date stamp in the sentence.

### E2 — Why a node is red, and what "Install Missing" cannot see
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Missing/red nodes: the workflow uses a custom node pack you lack"` (extend)
- **Change:** ADD (append to that paragraph)
- **Current text:** ends `…or via --enable-manager-legacy-ui.`
- **Proposed text:** ` Official causes, in order: you are not on a recent enough ComfyUI, or a pack FAILED TO IMPORT at startup — search the console for "IMPORT FAILED" and "Cannot import … module for custom nodes:" before reinstalling anything, and restart after installing (node classes register only at startup). Four more real causes: a frontend-extension conflict (official first move is Settings → Extensions → disable all third-party extensions, then reload — no restart needed); a blocked network, which shows as "Cannot connect to comfyregistry." and "version unknown" (set Manager network_mode = private or offline); a core-version comparison bug that fires when you are already newer (ignore it); and the folder name under custom_nodes/ no longer needing to match the GitHub repo name. IF "INSTALL MISSING CUSTOM NODES" LISTS NOTHING WHILE NODES ARE RED, the pack is probably not in the registry and is omitted SILENTLY — read properties.aux_id in the JSON to find the repo, then git clone into custom_nodes/ and install its requirements. USER`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Why a node can be installed and still red*; proposed lines 3, 5, 6; Manager#3138 (open), Manager#2030 (closed, log verbatim)
- **Grade:** OFFICIAL (docs/source) + USER (the two trackers)
- **Risk:** None.

### E3 — `cnr_id` / `aux_id` / `ver`, and the honesty limit
- **Target:** `NEW KNOWLEDGE line in ¶COMFYUI FAQ`
- **Change:** ADD
- **Current text:** none
- **Proposed text:** `Workflow JSON node properties: cnr_id = the registry pack id (core nodes use the literal "comfy-core"); aux_id = "github-user/repo" for packs NOT from the registry; ver = semver, a 4-40-char git hash, or literally "unknown". Most nodes carry none of it, so detection is PARTIAL by construction — never promise "drop your workflow and I'll list everything you need". Hand-editing these produces exact errors: "Node pack version has invalid semantic version", "Node pack version has invalid Git commit hash", "Invalid aux_id: Must be valid 'github-username/github-repo-name'". OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Registry packs vs git-cloned packs* (frontend zod schema verbatim); proposed line 4; §*Nothing-found* #2
- **Grade:** OFFICIAL (frontend source)
- **Risk:** None; the "partial by construction" clause is the load-bearing part for the app's own export claims.

### E4 — The corrected OOM ladder
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Out of memory, in order: official fp8 build of the same model"`
- **Change:** REPLACE
- **Current text:** `Out of memory, in order: official fp8 build of the same model (about half the size; GGUF smaller still but needs ComfyUI-GGUF, and ComfyUI itself recommends native fp8 over GGUF when dynamic VRAM is on); lower resolution/batch; VAE Decode (Tiled) node; --reserve-vram 2 / --disable-smart-memory / --lowvram / --novram / --cpu (last resort). --normalvram NO LONGER EXISTS, and --lowvram is a no-op under dynamic VRAM.`
- **Proposed text:** `Out of memory, corrected order (the docs' own ladder is now partly wrong): (1) lower resolution, frame count/length and batch size — VRAM scales with pixels × frames; (2) use the QUANTISED BUILD THE OFFICIAL TEMPLATE LOADS — int8_convrot for LTX-2.5 and H3, nvfp4 for H3's encoder, fp8mixed for FLUX.2 dev, fp8 for Qwen (20GB vs 41GB bf16) — these are the shipped default now, not an optimisation you add; (3) swap the final VAE Decode for VAE Decode (Tiled) (tile 512 / overlap 64; video temporal_size 64 / temporal_overlap 8) — video decode is very often the peak; (4) --cpu-vae when decode alone is the problem (reliable, slow); (5) --reserve-vram 2 / --vram-headroom / --disable-smart-memory; (6) --async-offload, --fast-disk, --cache-none to spend RAM and disk instead of VRAM; (7) ONLY THEN the VRAM group — and know the cost: --novram, --highvram, --gpu-only and --cpu each TURN DYNAMIC VRAM OFF, and --lowvram is a documented no-op while it is on. --normalvram does not exist. Block swap is not a core feature — it lives in the Wan and LTX wrapper packs, so using it means leaving the native graph. OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Running out of memory* (`enables_dynamic_vram()` verbatim); proposed lines 10, 11; error table #6
- **Grade:** OFFICIAL (source + docs)
- **Risk:** None — the current line sends students to `--novram` at step 4 of 5, which disables the system that was streaming their weights.

### E5 — Model paths students will get wrong in 2026
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Model folders: each loader reads exactly one folder under models/"` (extend)
- **Change:** ADD (append)
- **Current text:** ends `…restart after.`
- **Proposed text:** ` NEW FOLDERS 2026 MODELS NEED: models/latent_upscale_models/ (the LTX-2.5 spatial upscaler) and models/embeddings/ for MiniMax H3 "embedding:name" prompts (core v0.34.0). LTX-2.5 and H3 each need TWO VAEs, video and audio. FLUX.2 dev uses a MISTRAL text encoder, not T5 or Qwen. WAN VAEs ARE NOT INTERCHANGEABLE: 14B T2V/I2V load wan_2.1_vae.safetensors ("compatible with Wan2.2"), the 5B TI2V loads wan2.2_vae.safetensors — and the official I2V step list still names the t2v files, so trust the download cards, not the numbered steps. The missing-models popup only checks the TOP-LEVEL folder, so a model inside models/diffusion_models/wan_video/ triggers a false "missing" — ignore it and pick the file in the loader. GGUF files never get a download link at all (.gguf is treated as an unsafe format). OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Model files & paths* (per-family map, the re-verified I2V doc defect); proposed lines 7, 8, 9
- **Grade:** OFFICIAL
- **Risk:** None. This also resolves an open `[NOT FOUND]` in `comfyui-painpoints.md` §4.3 — the docs now state the correct VAE pairing unambiguously, though still not the failure symptom, so do not claim one.

### E6 — Metadata, Dev mode, subgraph values
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Loading workflows: drag the .json onto the canvas"` (extend)
- **Change:** ADD (append)
- **Current text:** ends `…is only for driving the server from code.`
- **Proposed text:** ` API-format export is still behind DEV MODE (Settings → Dev Mode → "Enable dev mode options (API save, etc.)", default Disabled). ComfyUI embeds prompt + workflow in everything it saves unless launched with --disable-metadata (Desktop: "Disable saving prompt metadata in files"), and since core v0.30.0 MP4 tags are written at the START of the file, so recovery survives partial reads — Prompt Studio's Meta Inspector reads all of these. SUBGRAPHS: the values you typed live on the INSTANCE node — edit them via "Edit Subgraph Widgets" in the parameters panel (core v0.3.66+) without entering the subgraph; the inner node's widgets_values may still show stale template text, and the API prompt has the truth. Subgraph Blueprints are COPIED, not linked — editing one instance does not change another. Two save settings change what a recovered file looks like: "Save node IDs to workflow" (on) and "Sort node IDs when saving workflow" (off) — node-id churn is why two exports of "the same" workflow diff badly. OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Outputs, metadata, recovery*; proposed lines 14, 15, 16 · cross-references `research/_addenda/comfyui-metadata.md` (do not restate it — link it)
- **Grade:** OFFICIAL
- **Risk:** None. This also corrects our own `comfyui-faq-2026-08-28.md`, which guessed the dev-mode toggle was "possibly obsolete".

### E7 — Partner nodes are paid cloud calls
- **Target:** `NEW KNOWLEDGE line in ¶COMFYUI FAQ`
- **Change:** ADD
- **Current text:** none
- **Proposed text:** `PARTNER NODES (formerly "API nodes") are paid cloud calls, not local models. Four tells: a pricing badge (Settings → Partner Nodes); they need a Comfy account with credits; they only work from 127.0.0.1 unless you log in with an API key, so they will not work behind --listen on a LAN; and the docs live in a separate tree (/tutorials/partner-nodes/… vs /tutorials/video/…) — the SAME model name appears in both for LTX-2.5 and MiniMax H3. They send your prompts and images to third parties. Wan 3.0, Qwen-Image 3.0 and Flux 3 are partner-only. --disable-api-nodes removes them and blocks all frontend internet access. Note the trap: docs.comfy.org's own H3 template blurb says "up to 2K resolution… roughly 15 seconds", which describes the PARTNER node — the local weights are 768p. OFFICIAL`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Telling a local node from a paid Partner node*; proposed line 17 · `research/new-models.md` §*New Comfy-Org templates* (the H3 blurb) · verification **K15**
- **Grade:** OFFICIAL
- **Risk:** None; the 2K trap is exactly the confusion the H3 GOTCHAS card already half-covers.

### E8 — Seeds, caching, and reproducibility limits
- **Target:** `KNOWLEDGE ¶COMFYUI FAQ "Seeds: control_after_generate"` (extend)
- **Change:** ADD (append)
- **Current text:** ends `…same seed + same settings = instant cached repeat, not a bug.`
- **Proposed text:** ` control_after_generate is CLIENT-SIDE ONLY (serialize: false) — it never reaches the server and does nothing if the seed widget has an incoming link; whether it fires before or after the run is Settings → Node Widget → Widget control mode. Use "fixed" while you iterate on wording, "randomize" only to explore. Queue count = N sequential runs at the same VRAM; batch size = one run at N× VRAM — when a student OOMs on "4 images", batch size is the fix. SYNTHESIS: the same seed on another machine will not give the same pixels. The seed fixes the noise, not the arithmetic — attention backend (Sage/Flash/cuDNN/PyTorch, with silent runtime fallbacks), weight dtype by GPU generation and quantisation all change it, and ComfyUI's own --deterministic help text hedges ("might not make images deterministic in all cases"). Reproduce within one machine and build; never grade on pixel identity.`
- **Evidence:** `research/_addenda/comfyui-ops-2026-09.md` §*Teaching gotchas*; proposed lines 18, 19, 20; §*Nothing-found* #8 (no official cross-GPU statement exists)
- **Grade:** OFFICIAL (the seed/queue mechanics) / SYNTHESIS (the cross-machine claim, explicitly labelled)
- **Risk:** The cross-machine claim has no single official source; its own file grades it `[SYNTHESIS]` and it is folded with that label intact.

---

## F — 08-28 backlog status

All ten "Recommended incorporations (ranked — not applied)" from
[`2026-08-28-digest.md`](../research/digests/2026-08-28-digest.md), with current disposition.

| # | 08-28 item | Status | Why |
|---|---|---|---|
| **F1** | H3 licence block (territorial, $20M, attribution, no-distillation) — "verify against the HF LICENSE first" | **Still valid, verified, and larger than stated** → **B6**, **H1** | Verified clause by clause against the HF `LICENSE` (#48, the sweep's strongest block). Two clauses the backlog did not anticipate matter more for a US-authored app: **§V.4 + Exhibit A #1 extend the ban to OUTPUTS**, and §I.11 makes any LoRA a Model Derivative. Also: attribution is two obligations, only one mandatory. |
| **F2** | Krea 2 length ceiling as a hard validator rule + expander warning | **Still valid, and now sourced rather than contested** → **A27**, **D5** | The backlog said the cap was a `[TESTED]` community observation; `encoder.py`'s `max_length = 512` is now the vendor's own code (#54). The app's current line asserting "no cap exists anywhere in the source" is the opposite of the source and must go. |
| **F3** | Weighting-syntax audit table | **Superseded — the mechanism was wrong** → **A28**, **A37** | The table's Qwen3-encoder row said scaling one token "shoves the whole conditioning (destructive past 1.2)". The stock tokenizers do not parse weights **at all** — they are literal text. Add two rows the backlog lacked: H3 is architecturally inert (`disable_weights=True`), and `{a|b}` on H3 is consumed by ComfyUI's wildcard expander. |
| **F4** | Promote "front-load the subject / order carries emphasis"; demote "write prose because the encoder is an LLM" | **Partly refuted; promote with two scopes** → **A33**, **A34** | The paper says 65% vs 70–90% — *"coming close to"*, not "matched" — and singles out FLUX.2 as still context-sensitive, with DrawBench Text at 24–37%. The demotion must be **task-scoped**, not universal, and the rule must never ship without the **U-Net limit** (SDXL 4%, SD 2.1 0.2%) while an SDXL target exists. "Four vendor guides converge" is uncounted; one is verified. |
| **F5** | H3 cloud/local split into the hidden-rewriter warnings | **Still valid, strengthened** → **E7**, and the existing H3 GOTCHAS card | Context-IR and Regenerate-2K confirmed cloud-only three ways. Add the trap the backlog missed: **docs.comfy.org's own H3 page and template blurb say "up to 2K resolution… about 15 seconds"** while the local weights are 768p — so the student's confusion comes from official docs. |
| **F6** | Qualify the H3 hardware and LoRA lines with precision and platform | **Still valid, now exactly quantifiable** → **H2**, and a KNOWLEDGE edit | MiniMax publishes **未公布** (no minimum VRAM) twice on its own self-hosting page; the 12 GB / ~42 GB pair is the marketing FAQ's 「社区验证过的参考点」 for **480p with audio**. Exact byte sizes are now known, and **"62+62 GB" is wrong** — bf16 model + bf16 encoder is **62 + 48 GiB**, and you need only one diffusion checkpoint at a time. The ~42 GB figure is arithmetically exactly the int8 ComfyUI stack (20.97 + 15.69 + 5.21 + 0.61 = 42.48). LoRA line: three turbo LoRAs now exist including a **Ref2V** one, so "no Ref2VA turbo LoRA" is false and "4-step Turbo v0.1" is stale. |
| **F7** | LTX-2.5 mechanical constraints; flag Auto Duration + Native Multishot as untested prompt-semantics changes | **Partly superseded** → **A7**, **B12**, **C4**, **D7** | Constraints confirmed (`8n+1`, ÷32, gated repo) — but **"$10M ARR" is "annual revenues"**, **Auto Duration is not an untested unknown, it is simply ABSENT from the ComfyUI template** (a Python-path feature; `num_frames=None` in Diffusers), and **Native Multishot has a fully documented prose syntax** with four per-cut requirements. Neither is an unknown any more; both are teachable. |
| **F8** | FLUX.2 / Z-Image negative-prompt trap | **Refuted in half** → **A32** | `ZImagePipeline` **does** have a string→negative path (#36). The dead-field claim survives for **klein only**, where the negative is hardcoded to `""`. Do not extend it to Z-Image, and drop "the same bug wearing a third hat". |
| **F9** | Add Wan-Dancer-14B to scope; add FLUX 3 to the closed-tier register | **Still valid, but both of its numbers must be dropped** → **A21**, **C2** | Wan-Dancer is in scope and its dialect is now fully documented — but **"Prompt Alignment 9.03" and "~28GB bf16" exist on no surface** (card, arXiv v2 abstract, project page all checked, #45/#47), and the app states both as fact. It is a caption schema, not "real prompts" in the cinematic sense. FLUX 3 confirmed closed, plus FLUX 3 Video as an API-only partner tier. |
| **F10** | Chinese vocabulary tables, after diffing the Wan 2.2 constants | **Still valid, diff done, and the bundle must be split** → **B9**, **A22**, and the digest's Chinese section | The diff was performed: cn-sweep §4.2 is **confirmed character-for-character** and is promoted to `[OFFICIAL]`. Two fixes to the backlog item itself: the constants live in **`system_prompt.py`**, not `prompt_extend.py`; and **`magic_prompt` is Qwen-Image's dead code, not Wan's** — the two findings were bundled and must be separated. `tar_lang="zh"` is confirmed as Wan's default. Four newly documented axes (对比度, 饱和度, 镜头焦段, 镜头类型) join the table, and the `[CM]` rows still lack URLs and access dates (#73). |

---

## G — Deferred: needs the test kit first

These are real, useful, and **not** fold-in material at their current grade. Each names the test that
would settle it. Kit: [`research/_addenda/test-kit-2026-09.md`](../research/_addenda/test-kit-2026-09.md).

| # | Claim | Grade | Test |
|---|---|---|---|
| **G1** | LTX-2.5's shipped negative is inert at CFG 1 on the distilled path | SPECULATION | **T2-LTX** — three arms (template as shipped / negative emptied / `NAGuidance` at `nag_scale` 5.0, `nag_alpha` 0.5, `nag_tau` 1.5). **Pixel-identical between arms (i) and (ii) is the only proof of inertness.** Note the arm is *designed, not shipped as JSON* — inserting a node exceeds the kit's edit budget, and ComfyUI issue #12707 questions whether the built-in NAG node works at all. |
| **G2** | Krea 2's `VAEEncode→ReferenceLatent` chain is dead weight | TESTED vs TESTED | Not in the kit. Same seed, same reference, with and without the `ReferenceLatent` node — 1C calls it "the single highest-value in-house test to run". |
| **G3** | Krea 2 Turbo is CFG-free | OFFICIAL vs OFFICIAL (unresolved) | The `krea/Krea-2-Turbo` card's own diffusers snippet is `guidance_scale=3.5` (ruling #9). **T2-krea** measures liveness; a CFG sweep at 0.0 / 1.0 / 3.5 on the ComfyUI path decides whether "CFG-free" needs a route clause or is simply wrong. Do not pick a side yet (exclusion #25). |
| **G4** | Order vs prose vs labels — does structure matter beyond word identity and position? | TESTED/PAPER (image models only, DiT only, not Z-Image/Krea/Qwen) | **T1** — same seed, three encodings of identical content (prose / `Subject:/Lighting:/Camera:` labels / scrambled order), on Qwen-Image and Krea 2. Collapses several fold-in items into one rule if order dominates. |
| **G5** | ZH vs EN at fixed seed on Alibaba-family models | LORE (n=1, no seeds) | **T3** — ZH/EN fixed-seed pairs for Wan 2.2 and Z-Image. The corpus's Chinese-language position rests on glyph rendering and cultural vocabulary, never on a motion/aesthetics A/B. |
| **G6** | Wan's `不超过4种` EN aesthetic-token cap is a model limit rather than a style choice | OFFICIAL instruction, UNSUPPORTED as a measured band (#40) | **T4** — the vendor's own EN exemplar carries ten. Until this runs, the 4 is advisory only (**D6**). |
| **G7** | `固定镜头` vs `固定机位` vs "static shot"; arc vs orbit | OFFICIAL token (#42); the orbit/45° rule is UNSUPPORTED | **T5** — seven files, two seeds. Settles both the token choice and whether "orbit" is genuinely a failure word. |
| **G8** | Where exactly Krea 2's cliff sits on the build students run | TESTED on INT8 ConvRot only, n=1 | **T6** — do the tokenizer count before queuing; keep the black outputs, which still carry full metadata and are the primary evidence. |
| **G9** | The five SDXL family recipes, head to head | OFFICIAL/CREATOR per card, never compared | **T7** — run first (cheapest, ~5 min). **Fix defect #67 first:** the shipped v-pred arm has no `ModelSamplingDiscrete` (v_prediction) node and no CFG-Rescale node, so as built it compares samplers on a v-pred checkpoint without the plumbing `sdxl.md` itself mandates. Adding the node breaks the "node ids byte-identical" guarantee — say so in the results. |
| **G10** | Z-Image negative-prompt liveness | OFFICIAL mechanism, untested behaviour | **T2-zimage** — but note defect #68: the "negative = positive" arm mathematically cancels guidance (`pred = pos + 4·(pos − pos) = pos`), so it measures "is the uncond branch computed at all", not "does the negative do anything". State which question each arm answers; for the semantic question, add a `CLIPTextEncode` wired to the sampler's negative input. |
| **G11** | Bernini-R: does it fit 24 GB at fp8, and does the shipped lightx2v cfg-step-distill LoRA make negatives inert? | LORE (wrapper README) / prediction | Not in the kit. Both are prerequisites for adding Bernini-R as a target beyond the `KNOWLEDGE` entry (**C3**). Also unresolved: `t2i`/`i2i`/`mv2v` appear in the upstream README but not in the ComfyUI tutorial's task table. |
| **G12** | Wan-Dancer's amplitude ladder is 低 / 中等 / 高 | SPECULATION | All five shipped prompt files use `高 / 中等 / 中等`; the other rungs are **not attested** in any file fetched. Reading the paper PDF body (not read this run) or a fixed-seed sweep of the slot values would settle it. |
| **G13** | Does the SCAIL-2 prompt do anything at all, and how much? | OFFICIAL direction, no controlled experiment anywhere | Not in the kit; specified in `scail2.md` §*Nothing-found*: fixed seed, fixed drive and mask, Replacement mode, four prompts — empty / `"The girl is dancing"` / a 110-word official-style prompt / the same 110 words with the background nouns removed. This is the only way to grade **A18** and **A19** above vendor assertion. |

---

## H — Licence & safety notices

Only items with a quotable source. Each is a candidate for a `KNOWLEDGE` clause, a GOTCHAS card, or
`wfNotes` on the relevant export.

- **H1 — MiniMax H3, the full clause set.** → folded by **B6**. The two clauses the app has never
  carried are the ones a US-authored teaching app most needs: **§V.4 and Exhibit A item 1 forbid using,
  reproducing, modifying, distributing or displaying the Works *or any of their Outputs* outside the
  Applicable Territory**, and §IV.2 is a **shall** while the "Powered by" line is only encouraged.
  `research/minimax-h3.md` item 1 · verification **#48**. Grade **OFFICIAL**.
- **H2 — An H3 LoRA is a Model Derivative.** §I.11 covers "any other machine learning model created by
  transferring the patterns of the weights, parameters, operational patterns, or Outputs … including by
  distillation methods, methods using intermediate data representations, or methods based on training
  using synthetic-data Outputs", so a LoRA inherits every term including the territorial ban on
  outputs. Several HF H3 LoRAs are nonetheless tagged `apache-2.0`. **Teach the derivative-inherits-the-
  licence rule; never infer permission from an uploader's tag.** Celebrity-likeness LoRAs additionally
  sit against Exhibit A item 13. `research/minimax-h3.md` §*LoRA & embedding ecosystem* ·
  verification **K14**. Grade **OFFICIAL** (licence) + **SPECULATION** (the compliance observation).
- **H3 — NoobAI-XL bans commercialisation outright.** Licence `fair-ai-public-license-1.0-sd` with an
  explicit *"II. Commercial Prohibition — We prohibit any form of commercialization"* plus a mandatory
  open-sourcing clause. Verifier's own finding, **absent from `sdxl.md` too** (#K27, severity High).
  Grade **OFFICIAL**. **Re-verify the licence text before folding** — it is the one item here that no
  Wave-1 file carries.
- **H4 — Pony V6 forbids monetized inference.** A modified FAIPL that bans running inference on any
  monetized site or app, with an explicit carve-out only for Civitai and Hugging Face. Same source and
  caveat as H3. Grade **OFFICIAL**.
- **H5 — Animagine XL 4.0 permits commercial use.** CreativeML OpenRAIL++-M. Worth stating alongside H3
  and H4 so the notice reads as guidance rather than blanket discouragement. Grade **OFFICIAL**.
- **H6 — LTX: "annual revenues", not ARR, plus the competing-model clauses.** → folded by **A13**.
  Attachment A **#18** (no training a competing model), **#20** (no use in a product that directly
  competes with Lightricks), **#5** (disclose machine-generated content) and **§6** (do not remove
  watermarking/provenance features, citing the EU AI Act and the California AI Transparency Act).
  Prompt Studio is a teaching app, not a competing video model — but #18/#20 deserve a GOTCHAS line for
  students planning products. `research/ltx23.md` item 15. Grade **OFFICIAL**.
- **H7 — FLUX.2 klein 9B is non-commercial AND requires filters.** klein **4B is Apache 2.0**; **9B and
  9B KV are FLUX Non-Commercial**, and the model card's Responsible AI §5 states filters or manual
  review **must** be used with the 9B models. `research/flux.md` item 6. Grade **OFFICIAL**.
  ⚠ `flux2_overview` was NOT re-fetched by the verifier (#29) — re-check before folding.
- **H8 — Krea 2's thresholds are unverified; its filter obligation is not.** → folded by **A29**. The
  content-filtering requirement is confirmed on the card; the "$1M revenue / 50 seats" figures appear on
  no surface any agent fetched (#K40). **Open `LICENSE.pdf` or state the thresholds as unverified.**
  Grade **OFFICIAL** (filter) / **unsourced** (thresholds).
- **H9 — Two unresolved licence conflicts to state rather than resolve.** (a) **SCAIL-2**: the code repo
  is **Apache 2.0 ("Copyright 2026 Zhipu AI")** while every distribution surface — HF, ModelScope,
  `Comfy-Org/SCAIL-2` — is tagged **MIT** (#8). Both permissive; state the conflict.
  (b) **H3 vs Comfy**: the licence grants royalty-free commercial use inside the Applicable Territory,
  while docs.comfy.org says *"Commercial use of locally generated outputs requires a MiniMax commercial
  license, available through Comfy, the only official reseller"* (#49). The likely reconciliation —
  Comfy is selling the §II route out of the Excluded Territories, which for a US/EU/UK/KR student is the
  only lawful path — is `[SPECULATION]`. **Teach the split, not either sentence.** Grade **OFFICIAL vs
  OFFICIAL**.

---

## Applying this

Suggested order: **A1–A3** and **A4–A6** first (both are live mis-teachings with verified corrections
and no dependencies), then the rest of **A**, then **D** (the validators depend on A's corrected facts),
then **B**, **E** and **H**, then **C** last — `wanAnimate2` and `wanDancer` are cheap because they
inherit Wan's vocabulary, while Bernini-R needs its sources re-fetched first (**C3**, **G11**).

Nothing in **G** should reach the bank until its named test has run and the results file
(`research/_addenda/test-kit/results-2026-09.md`) exists. Fix test-kit defects **#67** and **#68** before
running T7 and T2 respectively.
